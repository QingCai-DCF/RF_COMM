# RF_COMM plan 完成度审计

审计时间：2026-06-26 01:25 左右

## 1. 强约束状态

- 已复核 `项目约束(目标）.txt`，本轮未修改。
- SHA256：`CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`
- 本轮操作仍遵守该文件作为项目硬约束的要求。后续如需修改目标约束，必须先询问并取得确认。

## 2. 本轮仿真门禁结论

离线 simulation gate 已恢复为当前全量 PASS，并且此前临时隔离的两个 P2 smoke 项已经重新纳入 `-Test all`。本轮还新增了可独立运行的 PC/PS RFCM mock server，使 acceptance wrapper 能在无板状态下覆盖 HELLO、STATUS、CONFIG、TX/RX_DATA、CSV 二次分析和 TCP reconnect。

最终通过证据：

| 文件 | 结论 |
|---|---|
| `reports/simulation_gates_20260626_012712.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| `reports/simulation_gates_20260626_012712.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |
| `reports/simulation_gates_20260626_012712.err.log` | unittest 输出正常，整体门禁退出码为 0 |

通过项摘要：

| 项目 | 结果 |
|---|---|
| RTL XSim suite | PASS |
| `stream_2lane` / `stream_2lane_slow` | PASS，固定 2-lane 双向模型各方向 128 bytes |
| `stream_4lane` | PASS，固定 4-lane 双向模型各方向 128 bytes |
| `stream_ack_loss` | PASS，单次 ACK 丢失恢复模型通过 |
| 自动寻路旋转压力模型 | PASS，包含 20 cm 直径、600 rpm 旋转条件的模型化覆盖 |
| 2 小时旋转 soak 模型 | PASS，模型时间 7200 s |
| `stream_parallel_top_asym_2lane_perf` | PASS，作为 legacy 兼容入口映射到维护中的 asymmetric 2-lane perf bench |
| `stream_axi_bidir` | PASS，AXI wrapper 双端 queued bidir smoke 通过，A->B 90 bytes，B->A 75 bytes |
| PS bridge 静态检查 | PASS，`checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1` |
| host Python 编译 | PASS |
| host mock protocol tests | PASS，17 个 unittest OK，包含独立 mock server acceptance flow |
| offline acceptance wrapper | PASS，`offline_mock` 完成 8 包 TX/RX echo、CSV `ACCEPTANCE_PASS`、2 次 TCP reconnect，并已作为 `[PASS] host_offline_acceptance` 纳入 simulation gate |

运行脚本 `tools/run_simulation_gates.ps1` 会把当前进程设为 High priority，并设置 `ProcessorAffinity=65535`，这符合之前关于 14700KF 尽量使用高性能核心、多核运行的要求。

补充说明：`reports/simulation_gates_20260626_005334.*` 中曾出现 host mock unittest transient failure。本轮定位到 PC client 的 tracked send 存在 race：localhost mock 回复太快时，ACK/STATUS 可能先于 pending 登记到达，导致假 pending timeout。已修复为先登记 pending 再发送，发送失败时回滚。当前结论以 `reports/simulation_gates_20260626_012712.*` 为准。

## 3. 本轮修复内容

### 3.1 `stream_axi_bidir` 恢复

涉及文件：

| 文件 | SHA256 |
|---|---|
| `IPs/ip_ir_array/src/ir_array_top_axi.sv` | `4D69434FAC3F7089A63693E37A58ED3FFA16E89CE16861E61CD59431414846F2` |
| `IPs/ip_ir_array/sim/tb_ir_stream_axi_bidir_single_lane.sv` | `49DE1637CF9C44173570753C89B735FBE8E51F84237FC1D11906938061009727` |

修复结论：

- `ir_array_top_axi` 外层增加并向内传递 RX detect window 参数，使 AXI wrapper 的仿真采样窗口可以和已通过的 fixed stream bench 对齐。
- `tb_ir_stream_axi_bidir_single_lane` 改为使用 TFDU edge channel 模型，避免直接 `rx=~tx` 带来的物理模型不一致。
- testbench 增加 deterministic TDM backoff，保证单 lane 半双工同时排队 A/B payload 时不会退化成同一时刻反复碰撞。
- 通过日志：`sim_work/stream_axi_bidir_single_lane/stream_axi_bidir_single_lane.sim/sim_1/behav/xsim/simulate.log`
- 关键 PASS：`IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75`

### 3.2 `stream_parallel_top_asym_2lane_perf` 恢复

涉及文件：

| 文件 | SHA256 |
|---|---|
| `IPs/ip_ir_array/run_loopback_single_lane.ps1` | `77E6EC3030897AF9A62818659BD00F91BB41B70A3FA6A044D8DBDC7686901861` |

修复结论：

- 原 legacy 入口 `stream_parallel_top_asym_2lane_perf` 曾绑定到旧的 `ir_stream_parallel_2lane_top`，该 top 是 A-data/B-ACK 角色型结构，不适合作为对称双端 payload 性能 smoke。
- 现保留 legacy 测试名，但映射到维护中的 `tb_ir_stream_parallel_asym_2lane_perf`，覆盖同一类 asymmetric 2-lane 半双工 TDM 性能目标。
- 通过日志：`sim_work/stream_parallel_top_asym_2lane_perf_fixed_2lane/stream_parallel_top_asym_2lane_perf_fixed_2lane.sim/sim_1/behav/xsim/simulate.log`
- 关键 PASS：`IR_STREAM_PARALLEL_ASYM_2LANE_PERF_PASS`

### 3.3 PC/PS 离线 acceptance 恢复增强

涉及文件：

| 文件 | SHA256 |
|---|---|
| `software/host_client/rf_comm_client.py` | `8C7D2A908EE1C0AA49D83A8C42363BB2AB12A9C09FF4E05A09FA0DBA1E95606E` |
| `software/host_client/mock_rfcm_server.py` | `9F2C5042EE64BD49698D5E9A2141ABA5AC73CD3635F77579DE7924DE887E4624` |
| `software/host_client/run_acceptance.ps1` | `00426D9D4B6201F64C93ED78C182D571F924ACD6719C714CD7B3894FD57BDAF1` |
| `software/host_client/test_rf_comm_client.py` | `0DEA86DF4EB042FE2AC43C658F8F83886FA3EF68710E6BB8DA6720BF3E3AE241` |
| `tools/run_simulation_gates.ps1` | `28BCD83E713C30E3E4295FED51BA1A07747D9EF806C80D2EAD0A5325932EC46E` |
| `software/README.md` | `CAB084940823BC632EBDC3FE7B4D7F08F48041268078A3FB45C82D7D05406BD2` |
| `software/ps_lwip_bridge/README.md` | `051F9E3438B650A1E9B29F4BDD0482E4754CC5BDE61F7FE4353ACDF54110CF3B` |

修复和增强结论：

- `send_tracked()` 改为先登记 pending 再发送 TCP frame，消除低延迟 TCP 回复下 ACK/STATUS 先到导致 pending 永久残留的竞态。
- `run_reconnect_cycles()` 改为等待 HELLO/STATUS pending 清空，而不是固定 sleep 后直接统计，重连 smoke 的判据更接近真实协议行为。
- 新增 `mock_rfcm_server.py`，可作为本地 PS bridge mock server 独立启动。
- `run_acceptance.ps1` 新增 `offline_mock` 模式，自动挑选空闲 localhost TCP 端口，自动启动/停止本地 mock server，并执行 smoke、CONFIG、TX/RX echo、CSV 分析和 reconnect。
- `tools/run_simulation_gates.ps1` 已把 `mock_rfcm_server.py` 纳入 Python 编译检查，并把 `offline_mock` acceptance 纳入全量 simulation gate。
- 通过日志：`software/host_client/logs/offline_mock_single_lane_20260626_013119.csv`
- 关键 PASS：`ACCEPTANCE_PASS`，`acked_tx=8`，`errors=0`，`rx_data=8`，`pending=0`

## 4. plan 项完成度审计

| 项 | 当前状态 | 证据和说明 |
|---|---|---|
| P0-1 证据冻结和配置 diff | 完成 | `evidence_lock_20260625.csv`、`config_diff_known_good_vs_current.md` |
| P0-2 lane0 known-good replay | 完成 | `reports/lane0_hw_loopback_safe_20260625_230834.summary.txt`，`sent=11267`、`rx_ok=11267`、`tx_fail=0`、`loss=0.0%` |
| P0-3 2-lane known-good replay | 部分完成/受阻 | 非 parallel 2-lane 有 raw pulse 证据但功能失败；parallel 2-lane build 受 ZYNQ-7010 资源约束阻塞 |
| P0-4 session/mask mismatch 分层诊断 | 完成 | session match PASS；session mismatch 落入协议/CRC 类；mask mismatch 落入 D7 应用层 lane-mask mismatch |
| P0-5 RX microscope | 部分完成 | 已增加 D7/D8 应用层诊断和解码，但尚未做完整 header/CRC/RX FSM microscope |
| P0-6 ACK-only | 完成 | `reports/lane0_hw_loopback_safe_20260625_235911.summary.txt`，`B2A_ENABLE=0`、`B2A_FREE_RUN=0`，A 侧仍稳定完成，说明 lane0 ACK-only 路径成立 |
| P1-1 lane mapping matrix | 部分完成/停止条件触发 | Pin33 remap 后 lane1 A->B 仍无 B_RX 脉冲，证据见 `reports/2lane_prearmed_a_tx_lane1_20260625_225024.summary.txt` 和对应 ILA CSV |
| P1-2 lane1/2-lane 硬件恢复 | 未开始/暂停 | 按 plan stop condition，应先解决 lane1 B_RX 物理输入问题 |
| P2 PS/PC/TCP/DHCP 离线 smoke | 部分完成/增强 | PS bridge static、host mock、AXI wrapper bidir smoke、offline acceptance wrapper 均已 PASS；TCP reconnect/pending race 已修复 |
| P2 PS/PC/TCP/DHCP 真实端到端 | 未完成 | 尚未完成真实 PS 软件、真实 PC 上位机、真实 TCP/DHCP 网络链路和硬件通信的端到端验证 |
| P3 旋转实物/工业化验证 | 未开始 | 当前只有仿真模型和工程评估，不包含真实旋转轴系实测 |

## 5. 当前硬件安全状态

本轮只做离线仿真和文字记录，没有新增上板实物实验。

最近一次实物运行仍为：

- `reports/lane0_hw_loopback_safe_20260626_001109.summary.txt`
- `reports/program_tfdu_shutdown_after_lane0_loopback_20260626_001109.log`

该 shutdown 日志包含：

```text
TFDU_SHUTDOWN_PROGRAMMED C:/Users/user/Documents/RF_COMM/shutdown_bitstream/tfdu_shutdown_j10_j11.bit
```

因此当前记录上的最后实物状态是已经烧入 shutdown bitstream。

## 6. 下一步建议

建议分两条线继续推进：

1. 硬件恢复线：先不做 2-lane 性能，专门解决 lane1 B_RX 无脉冲问题。用最短安全运行窗口，只做物理通断和 ILA 采样，结束后立即烧 shutdown bitstream。
2. 协议集成线：在不碰硬件的情况下，继续把 offline mock acceptance 扩成更接近真实板端的 PC-to-PC smoke 证据；硬件可用后再用同一 wrapper 指向 DHCP 或 static fallback 地址。

当前不能宣称 2-lane 实物已经恢复，也不能宣称完整 TCP/DHCP 端到端已经完成。可以宣称的是：当前离线 RTL/host/PS static simulation gate 已经全量 PASS，lane0 实物闭环曾在当前记录中恢复，lane1/2-lane 实物仍受物理输入问题阻塞。

## 7. 2026-06-26 01:54 增量审计：单 lane 启动链和门禁闭合

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`，SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| P2-1 bit/XSA/BOOT 一致性 | 完成一轮离线闭合 | `reports/build_current_bitstream_lane0_active_20260626_014516.meta.txt`、`reports/build_boot_image_after_lane0_xdc_20260626_014810.meta.txt` |
| 项目门禁 | PASS | `reports/project_gates_after_lane0_xdc_20260626_014856.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS` |
| 完整仿真门禁 | PASS | `reports/simulation_gates_after_lane0_xdc_20260626_014918.out.log` 包含 `RF_COMM_SIMULATION_GATES_PASS` |
| Vivado 日志清洁度 | PASS | project gate 检查 `synth_1/runme.log`、`impl_1/runme.log`、`TFDU_VFIR_Client_Array/vivado.log` 均通过 |
| utilization 解析 | PASS | project gate 输出 `LUT=44.04% FF=24.89% BRAM=9.17% DSP=0.00%` |

当前制品状态：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `954563915AC0D483DC195D2AAD54119FA5090E87E585A67206EE22B3F34F2F92` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `51C7EC7497F8026346E8575EBDEA57CADEF3BF12684BD4C0DDD20A1C6F1B9F29` |
| `software/_boot/BOOT.BIN` | `6CF2DEF65DB0A40D7EC46F6486DA911E9248925CDCB9AB2AE047371A09687CC7` |

范围限定：

- 当前 Vivado 顶层仍是单 lane，有效 XDC 只启用 J10/lane0 约束。
- J11/lane1 映射保留为注释，包含 B_RX1 改到 J11 PIN33 / G15 的记录；这不是 lane1 已恢复的证据。
- 本轮没有新增实物实验，最后记录的硬件状态仍是 shutdown bitstream 已烧入。

审计结论更新：

- 可以宣称当前单 lane 工程启动链、项目门禁和完整离线仿真门禁已闭合。
- 仍不能宣称 2-lane 实物恢复，也不能宣称真实 PS-PC TCP/DHCP 端到端完成。
- 下一步若继续硬件线，应先生成真实 2lane 顶层、启用 J11/lane1 约束，并在 5 分钟安全窗口内只做物理通断/ILA 验证，结束后立即烧入 shutdown bitstream。

## 8. 2026-06-26 02:07 增量审计：P0-5 RX microscope 强化

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`，SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| P0-5 RX microscope AXI 可读化 | 部分完成并明显增强 | 新增 `axi_rx_microscope` 仿真，AXI debug 可读出 session/header 分层状态 |
| RX 数据接收/ACK 排队诊断 | 完成一层增强 | `ir_array_rx_mgr.sv` 新增 `0xD9` debug code，`decode_p0_debug.py` 已可解码 |
| 仿真门禁 | PASS | `reports/simulation_gates_rx_microscope_20260626_015858.out.log` 包含 `RF_COMM_SIMULATION_GATES_PASS` |
| 当前 bit/XSA/BOOT 重建 | PASS | `reports/build_current_bitstream_rx_microscope_20260626_020413.meta.txt`、`reports/build_boot_image_rx_microscope_20260626_020656.meta.txt` |
| 项目门禁 | PASS | `reports/project_gates_rx_microscope_20260626_020730.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS` |

关键仿真证据：

```text
AXI_RX_MICROSCOPE_SESSION_MISMATCH_PASS sticky=0x00000010 rx_good=0x00000001 debug=0xd4201203
```

判读更新：

- `sticky=0x00000010`：session mismatch 被归入 header/session 层，不再只是粗略的 RX fail。
- `rx_good=0x00000001`：接收侧确认看到过有效接收事件，能区分“有接收但协议/session 不匹配”和“物理层完全无输入”。
- `debug=0xd4201203`：AXI debug 寄存器可提供 P0-5 需要的 microscope 线索，后续可和 ILA 采样、真实板级寄存器读数对照。

当前制品状态：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `E21197652D31800386D7A72DB1EC8FA6B26E87D43087F64002B9FE5C48EC8986` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `F374C50005561257005CE6C02CD8503369A38B82BE92457F037DBDE3DA012FBB` |
| `software/_boot/BOOT.BIN` | `6CF2DEF65DB0A40D7EC46F6486DA911E9248925CDCB9AB2AE047371A09687CC7` |

范围限定：

- 本轮没有新增实物实验，也没有烧写开发板；最后记录的硬件状态仍是 shutdown bitstream 已烧入。
- 当前有效 Vivado 顶层仍是单 lane，不能据此宣称 lane1/2-lane 实物恢复。
- P0-5 仍未完全覆盖所有真实硬件复杂故障，例如真实光路边沿畸变、lane1 物理输入异常、长时间旋转/遮挡扰动下的 RX microscope 统计。

审计结论更新：

- 可以宣称 P0-5 的离线可观测性已经显著增强，并且该能力已经进入当前 bit/XSA/BOOT 制品。
- 可以宣称当前离线 RTL/host/PS static 仿真门禁和项目门禁仍为 PASS。
- 仍不能宣称完整 plan 已完成；未完成项仍包括 lane1/2-lane 真实物理恢复、真实 PS-PC TCP/DHCP 端到端、旋转轴系实物/长时间运行验证。

## 9. 2026-06-26 02:30 增量审计：真实 2-lane 顶层和 J11 约束离线闭合

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`，SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 真实 2-lane BD/Wrapper | 完成 | `design_shiboqi_wrapper.v` 已暴露 `ir_*[1:0]` 和 `loop_*[1:0]` |
| J11/lane1 XDC 约束 | 完成 | `PORT1.xdc` 已启用 lane1 A/B 约束，`B_RX1=G15` |
| 2-lane bit/XSA/BOOT | PASS | `reports/build_current_bitstream_2lane_stream_bidir_rxmicroscope_20260626_021323.meta.txt`、`reports/build_boot_image_2lane_stream_bidir_rxmicroscope_20260626_021717.meta.txt` |
| 项目门禁 | PASS | `reports/project_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022203.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS` |
| 完整仿真门禁 | PASS | `reports/simulation_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022622.out.log` 包含 `RF_COMM_SIMULATION_GATES_PASS` |

当前 2-lane 制品状态：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `E4787A9E73E855ED9D7582B4AE1B34CFE77835E418AC4DA2316E4CD33DF17382` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `EBF2C2D2CC985FA31B4858E0FB7CF20E6DFDAFDDC89911A4B0559659AD02D7B6` |
| `software/_boot/BOOT.BIN` | `C6F4A05A0DEC7D6270D9BF2BD5E8E75533D9B2EEC633A8F3B65F23FF4746EF23` |

资源和时序：

| 指标 | 结果 |
|---|---|
| Timing | `WNS=1.218`、`WHS=0.018`、`setup_fail=0`、`hold_fail=0` |
| Utilization | `LUT=61.27%`、`FF=32.83%`、`BRAM=9.17%`、`DSP=0.00%` |

状态更新：

| plan 项 | 旧状态 | 新状态 |
|---|---|---|
| P1-1 lane mapping matrix | 部分完成/停止条件触发 | 2-lane 构建前置已恢复；真实 mapping matrix 仍未上板验证 |
| P1-2 lane1/2-lane 硬件恢复 | 未开始/暂停 | 已具备 2-lane 可上板镜像；实物恢复仍未证明 |
| P2-1 BOOT.BIN | 单 lane 镜像闭合 | 当前 2-lane stream_bidir 镜像也已闭合 |

范围限定：

- 本轮没有新增实物实验，也没有烧写开发板；最后记录的硬件状态仍是 shutdown bitstream 已烧入。
- 当前结论只证明 2-lane stream_bidir 工程能综合、实现、生成 bit/XSA/BOOT，并通过离线门禁。
- 不能据此宣称 lane0/lane1 的真实 TFDU 光路都通，也不能宣称 2-lane 当前版本已经达到实物 `sent>=10000 loss<0.1%`。
- `IR_PARALLEL_2LANE_MODE=1` parallel 版本仍受资源约束，不是当前可用上板镜像。

审计结论更新：

- 可以宣称“真实 2-lane 端口和 J11/lane1 约束已恢复，2-lane stream_bidir 构建链离线闭合”。
- 不能宣称“2-lane 实物恢复完成”。下一步硬件线应在 5 分钟安全窗口内做 lane0/lane1 A-B、B-A 通断矩阵，并在结束后立即烧入 shutdown bitstream。
- 完整 plan 仍未完成；真实 PS-PC TCP/DHCP 端到端和旋转轴系/2 小时实物验证仍未完成。

## 10. 2026-06-26 02:45 增量审计：2-lane ILA 镜像完成，硬件窗口被 JTAG 枚举问题阻断

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`，SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 2-lane 被动 ILA | 完成 | `reports/vivado_add_2lane_phy_ila_current_20260626_023259.meta.txt`，`ADD_2LANE_ILA_EXIT code=0` |
| 带 ILA 的 2-lane bit/XSA/BOOT | PASS | `reports/build_current_bitstream_2lane_stream_bidir_ila_20260626_023331.meta.txt`、`reports/build_boot_image_2lane_stream_bidir_ila_20260626_023734.meta.txt` |
| 带 ILA 镜像项目门禁 | PASS | `reports/project_gates_2lane_stream_bidir_ila_20260626_024119.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS` |
| 2-lane 安全脚本错误收尾 | 完成 | `tools/run_2lane_hw_prearmed_ila_safe.ps1` 语法检查 PASS，失败时会记录 `RUN_ERROR` 和 shutdown 尝试 |

当前带 ILA 制品状态：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `96963E740D9B115C0E60A89B355C9EB775716F2DE06E30C0EB6048DF441DAA5B` |
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.ltx` | `32805D7AE4FDFB411F74E821A6CCF99702C879E825318548224640062F18913C` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `E7A137FA96507C1E1A3290B1A548711E4F560E68834FF490588946B8FFA3D17F` |
| `software/_boot/BOOT.BIN` | `4C753690E35F5D3ED2F611E3D83602BF4A94CE260E34FBF241F24378BBF7C30D` |

资源和时序：

| 指标 | 结果 |
|---|---|
| Timing | `WNS=1.635`、`WHS=0.01`、`setup_fail=0`、`hold_fail=0` |
| Utilization | `LUT=69.90%`、`FF=39.61%`、`BRAM=45.83%`、`DSP=0.00%` |

硬件窗口状态：

| 项 | 当前状态 | 证据 |
|---|---|---|
| `a_tx_lane0` 短窗口 | 未进入有效测试 | `reports/2lane_prearmed_a_tx_lane0_20260626_023755.summary.txt` |
| 失败点 | PS/JTAG init | `Cannot reset APU. Timeout waiting DAP to reconnect after reset` |
| 2-lane bit 是否烧入 | 未证明烧入，且日志未到 `Programming FPGA` | `reports/xsct_init_2lane_prearmed_a_tx_lane0_20260626_023755.out.log` 停在 `Resetting PS system` |
| shutdown retry | 未成功，因无 hw target | `reports/program_tfdu_shutdown_retry_after_failed_init_20260626_023946.log` 包含 `HW_TARGET_COUNT 0` |
| USB 枚举 | JTAG USB 和 COM3 可见 | Windows PnP 显示 `USB Serial Converter` 与 `CP210x USB to UART Bridge (COM3)` |

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P1-1 lane mapping matrix | 仍未完成；已具备 ILA 镜像，但硬件目标未枚举，未形成 lane0/lane1 A-B/B-A 证据 |
| P1-2 lane1/2-lane 硬件恢复 | 仍未完成；当前阻塞点是 JTAG/板卡 target 不可见，不是红外链路判定失败 |
| P2-1 BOOT.BIN | 带 ILA 的 2-lane 镜像已重建并通过项目门禁 |

审计结论更新：

- 可以宣称 2-lane 硬件验证所需的可观测镜像已经准备好。
- 不能宣称本轮完成了任何 2-lane 实物通断验证。
- 下一步必须先恢复 Vivado Hardware Manager 对 `xc7z010` 的枚举；在能看到 hw target 前，不应继续重复上板通信测试。

## 11. 2026-06-26 02:50 增量审计：无烧写预检门槛固化

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`，SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 无烧写 JTAG/板卡预检 | 完成 | `tools/check_hw_target.tcl`、`tools/check_hw_target.ps1` |
| 2-lane 安全脚本前置挡板 | 完成 | `tools/run_2lane_hw_prearmed_ila_safe.ps1` 默认先跑预检 |
| 当前硬件不可测试原因 | 已固化 | `reports/hw_target_preflight_20260626_024825.summary.txt` |
| 防误烧验证 | 完成 | `reports/2lane_prearmed_a_tx_lane0_20260626_024843.summary.txt` 包含 `PREFLIGHT_BLOCKED_NO_PROGRAMMING=1` |

预检证据：

```text
SERIAL_PORTS=COM1,COM3
COM_PORT_PRESENT=1
PNP_DEVICE status=OK class=USB name=USB Serial Converter id=USB\VID_0403&PID_6014\210512180081
PNP_DEVICE status=OK class=Ports name=Silicon Labs CP210x USB to UART Bridge (COM3)
HW_PREFLIGHT_TARGET_COUNT 0
HW_PREFLIGHT_RESULT FAIL_NO_TARGET
```

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P1-1 lane mapping matrix | 仍未完成；现在有明确前置门槛：`HW_PREFLIGHT_RESULT PASS` 后再测 |
| P1-2 lane1/2-lane 硬件恢复 | 仍未完成；当前不是红外链路失败，而是 Vivado 无 hw target |
| 硬件安全流程 | 增强 | 预检失败时不烧写、不启动 UART/ILA/ELF，不需要 shutdown |

审计结论更新：

- 可以宣称 2-lane 上板测试流程的前置安全门槛已经固化。
- 不能宣称任何新的实物通信验证完成。
- 当前下一步仍是恢复 Vivado Hardware Manager 对 `xc7z010` 的枚举；在预检 PASS 前，计划内硬件矩阵测试应保持暂停。

## 12. 2026-06-26 02:55 增量审计：JTAG target 复测仍失败

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 1 MHz 无烧写预检 | FAIL | `reports/hw_target_preflight_20260626_025118.out.log`，`HW_PREFLIGHT_TARGET_COUNT 0` |
| 500 kHz 无烧写预检 | FAIL | `reports/hw_target_preflight_20260626_025505.out.log`，`HW_PREFLIGHT_TARGET_COUNT 0` |
| 串口 | 可见 | `COM_PORT_PRESENT=1`，COM3 为 CP210x |
| USB-JTAG 相关设备 | Windows 可见 | `USB Serial Converter`，VID `0403`、PID `6014` |
| Vivado hw target | 不可见 | 两次均为 `HW_PREFLIGHT_RESULT FAIL_NO_TARGET` |
| FPGA 是否烧写 | 未烧写 | 仅执行 `check_hw_target.ps1`，未进入测试脚本烧写阶段 |

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P1-1 lane mapping matrix | 仍暂停；原因是 Vivado 未枚举到 hw target |
| P1-2 lane1/2-lane 硬件恢复 | 仍暂停；尚无新的 lane 通断证据 |
| 硬件安全流程 | 继续有效；预检失败时未烧写、未驱动 TFDU、不需要 shutdown |

审计结论更新：

- 可以宣称：JTAG/Vivado 目标不可见问题已经被复测确认，不是单次 hw_server 启动偶发。
- 不能宣称：当前完成了 lane0/lane1 AB/BA 物理通断复测。
- 下一步必须先恢复 Vivado Hardware Manager 对 `xc7z010` 的枚举；否则继续跑上板脚本没有工程意义，也会增加安全风险。

## 13. 2026-06-26 02:58 增量审计：JTAG/USB 诊断工具补齐

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| JTAG/USB 综合诊断脚本 | 完成 | `tools/diagnose_jtag_usb.ps1`，SHA256 `12854A79B5FE2DEF7B65A501CF91DAC577BD36F7981B4320B15F18F7915E288D` |
| 完整诊断报告 | 完成 | `reports/jtag_usb_diag_20260626_025747.summary.txt` |
| 快速脚本自检 | 完成 | `reports/jtag_usb_diag_20260626_025828.summary.txt` |
| 中文诊断说明 | 完成 | `JTAG目标枚举诊断_20260626.md` |

新增证据摘要：

```text
COM_PORT_PRESENT=1
FTDI6014_PRESENT=1
FTDI6014 current driver=oem27.inf / FTDI / FTDIBUS
Digilent driver package present=oem18.inf / dmodusb.inf
Xilinx cable driver package present=oem37.inf / xpcwinusb.inf
Vivado 1 MHz preflight=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
Vivado 500 kHz preflight=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
```

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P0 evidence/config diff | 已有 `evidence_lock_20260625.csv` 和 `config_diff_known_good_vs_current.md`；本轮新增 2026-06-26 JTAG 阻塞诊断证据 |
| P1-1 lane mapping matrix | 仍暂停；已有复测入口，等待 `HW_PREFLIGHT_RESULT PASS` |
| P1-2 lane1/2-lane 硬件恢复 | 仍暂停；当前缺少 hw target，不是 lane 通断结论 |

审计结论更新：

- 可以宣称：JTAG 阻塞的诊断入口和证据记录已经补齐。
- 不能宣称：JTAG 阻塞已经解决。
- 不能宣称：2-lane AB/BA 通断矩阵已完成。

## 14. 2026-06-26 03:03 增量审计：P1-1 ILA 矩阵判读工具

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 2-lane ILA CSV 离线判读工具 | 完成 | `tools/analyze_2lane_ila_csv.py`，SHA256 `BAD8CB3C9444D7407D60D865ED4478090EB60C59367248DADA09812003F9130E` |
| Python 语法验证 | PASS | `python -m py_compile .\tools\analyze_2lane_ila_csv.py` |
| 样例矩阵报告 | 完成 | `reports/ila_2lane_matrix_analysis_sample_20260626.md` |
| 最近 20 个 ILA 总览 | 完成 | `reports/ila_2lane_matrix_analysis_latest20_20260626.md` |

样例覆盖：

```text
known-good B->A: PASS_ANY_B_TO_A / B_TO_A_LANE0
2026-06-25 A->B lane0: PASS_EXPECTED_RAW
2026-06-25 A->B lane1 PIN33: FAIL_EXPECTED_RAW / FAIL_NO_RX_ACTIVITY
```

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P1-1 lane mapping matrix 工具链 | 离线判读工具完成 |
| P1-1 lane mapping matrix 实测 | 未完成；等待 JTAG target 恢复 |
| P1-2 lane1/2-lane 硬件恢复 | 未完成；仍需 `HW_PREFLIGHT_RESULT PASS` 后继续 |

审计结论更新：

- 可以宣称：用于 P1-1 的 ILA CSV 自动判读能力已经具备。
- 不能宣称：P1-1 的新实物矩阵已完成。
- 不能用 raw pulse pass 直接替代协议层闭环 pass；后者仍需 UART/AXI/CRC/session/ACK 证据。

## 15. 2026-06-26 03:08 增量审计：P1-1 矩阵安全总控入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 2-lane 矩阵安全总控脚本 | 完成 | `tools/run_2lane_matrix_safe.ps1`，SHA256 `FA2779BEA5823F2CE1403A64D81745DCD11C15F70893F2AAC83A9DB4EDD5CFC0` |
| 总控脚本语法验证 | PASS | `PARSE_OK .\tools\run_2lane_matrix_safe.ps1` |
| 总控预检挡板验证 | PASS | `reports/2lane_matrix_safe_20260626_030720.summary.txt` |
| 预检失败时不进入单次 run | PASS | `reports/2lane_matrix_safe_20260626_030720.summary.txt`，无同 stamp 的 `*.run.log` |

最终挡板证据：

```text
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
MATRIX_PREFLIGHT_PASS_PARSED=0
MATRIX_PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
```

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P1-1 lane mapping matrix 执行入口 | 完成；等待 JTAG target 后可直接运行 |
| P1-1 lane mapping matrix 实测 | 未完成；当前预检仍失败 |
| 硬件安全流程 | 增强；总控层和单次 run 层都有 preflight 挡板 |

审计结论更新：

- 可以宣称：P1-1 的矩阵执行入口、预检挡板、自动 ILA 分析串联已经准备好。
- 不能宣称：P1-1 的实物矩阵已经完成。
- 当前完成度仍受 JTAG target 不可见限制；恢复 JTAG 后下一步可直接运行总控脚本。

## 16. 2026-06-26 03:10 增量审计：PS/PC 离线通信门禁

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| PS/PC 离线门禁总控脚本 | 完成 | `tools/run_ps_pc_offline_gates.ps1`，SHA256 `7FA1DC27D476ED8DE99E4B9C577E165BDC38FF588A40C5B4E61909B64D3A5765` |
| PS bridge TCP/DHCP 静态检查 | PASS | `reports/ps_pc_offline_gates_20260626_031017.static.log` |
| host client 单元测试 | PASS | `reports/ps_pc_offline_gates_20260626_031017.unittest.log`，17 tests OK |
| offline mock acceptance | PASS | `reports/ps_pc_offline_gates_20260626_031017.acceptance.log` |
| 统一门禁 summary | PASS | `reports/ps_pc_offline_gates_20260626_031017.summary.txt` |

门禁摘要：

```text
PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1
Ran 17 tests ... OK
sent_packets=64 sent_bytes=6144
tx_packets=64 rx_data=64 rx_data_bytes=6144
errors=0 ack_timeouts=0
log_acceptance PASS
PS_PC_OFFLINE_GATES_PASS static=1 unittest=1 offline_mock=1
```

状态更新：

| plan 项 | 当前状态 |
|---|---|
| P2 PS/PC software protocol offline | PASS；TCP/DHCP/reconnect 源码静态约束和 host mock acceptance 已验证 |
| P2 PS/PC real board TCP/DHCP smoke | 未完成；需要真实 AX7010 PS 以太网和开发板运行 |
| P2 PC-to-PC through IR chain | 未完成；需要 PL/IR 2-lane 或至少 lane 闭环恢复后执行 |

审计结论更新：

- 可以宣称：PS/PC 软件协议和 host 工具的离线门禁已具备并通过。
- 不能宣称：真实 PS 到电脑的 TCP/DHCP 上板通信已完成。
- 不能宣称：电脑上位机到红外链路再回电脑的端到端链路已完成。

## 17. 2026-06-26 03:14 增量审计：自动完成度审计入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。

新增完成项：

| 项 | 当前状态 | 证据 |
|---|---|---|
| plan 完成度自动审计脚本 | 完成 | `tools/audit_plan_completion.py`，SHA256 `30A50A5463A01119A3AD0E6F5F8C08073BD05E5D2A8C86F9A4B1A9A92DAB424A` |
| Markdown 审计报告 | 完成 | `reports/plan_completion_audit_current_20260626.md` |
| JSON 审计报告 | 完成 | `reports/plan_completion_audit_current_20260626.json` |

审计结果：

```text
PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
NOT_STARTED=3
```

PASS 项：

```text
C0 hard constraint hash
P0 evidence/config diff
SIM-2L current RTL simulation gates
PG-2L-ILA current Vivado build gates
ARTIFACTS bit/LTX/XSA/BOOT/shutdown present
P1-1-TOOLS safe matrix runner and ILA analyzer
P2-OFFLINE PS/PC software protocol offline gates
```

WAITING_HARDWARE 项：

```text
JTAG target enumeration
fresh 2-lane physical matrix
real AX7010 PS-to-PC TCP/DHCP smoke
PC-to-PC through IR chain
current 2-lane protocol hardware restore
```

NOT_STARTED 项：

```text
stationary soak
20 cm / 600 rpm / 2 h rotation validation
4/8-lane final throughput expansion
```

审计结论更新：

- 可以宣称：当前 plan 状态已有可重复机器审计入口。
- 可以宣称：当前缺口不再是“证据不清楚”，而是明确集中在 JTAG/实物链路恢复及后续实测。
- 不能宣称：目标 plan 已完成；仍有 5 项等待硬件、3 项未开始。

## 18. 2026-06-26 03:16 增量审计：JTAG 重试仍等待硬件枚举

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只做无烧写预检和安全矩阵入口拦截验证。

新增证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| JTAG no-programming preflight | FAIL_NO_TARGET | `reports/hw_target_preflight_20260626_031534.out.log`，SHA256 `23910FB616CC747779AC3D175E165A5AD52DA0B5BFCC14F49F6C0DA6B7E80A09` |
| 2-lane safe matrix guard | PASS as guard / blocked as hardware test | `reports/2lane_matrix_safe_20260626_031555.summary.txt`，SHA256 `461EA1495B586E6047EF9663B7AB4F6EB0A2791D43E0EC44DE384498EE655CA4` |
| 自动完成度审计报告 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `F31A09482D49559B6C8709585F03A869F5A652F2F7FDA3B9D94C287F68AEB6D3` |
| 自动完成度审计 JSON | 已刷新 | `reports/plan_completion_audit_current_20260626.json`，SHA256 `7E7AC9A40C88E3020C37734E763E4516E3529CF8F7939F2854A519ACD7AEEB8A` |

关键判据：

```text
HW_PREFLIGHT_TARGET_COUNT 0
HW_PREFLIGHT_RESULT FAIL_NO_TARGET
MATRIX_PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
NOT_STARTED=3
```

审计结论更新：

- 可以宣称：2-lane 实物矩阵入口具备防误烧写保护，并在 JTAG target 不存在时正确停止。
- 可以宣称：本轮没有运行任何 TFDU 通信实验，未触发 5 分钟小板运行限制。
- 不能宣称：JTAG/Hardware Manager 问题已解决。
- 不能宣称：新的 lane0/lane1 A->B 与 B->A 物理通断验证已经执行。

## 19. 2026-06-26 03:18 增量审计：JTAG 诊断工具可靠性补强

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只修改诊断工具，不改变项目实现目标、RTL、XDC、PS 软件或 host 协议。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| `diagnose_jtag_usb.ps1` 跳过 Vivado 预检别名 | 已修复并验证 | `tools/diagnose_jtag_usb.ps1`，SHA256 `C66609A943B0E280B4BB3A9EAA3AE9987A8668E6179F21CE44E0AD7E2C4E58DB` |
| 快速 USB/驱动诊断 | PASS as diagnostic / no hardware programming | `reports/jtag_usb_diag_20260626_031859.summary.txt`，SHA256 `FE339B825F395D48E71B1734D03DD581F84B4A8880BAA34A9E92C6A922B563E5` |
| 当前 JTAG 问题定位 | 仍等待 Vivado target 枚举 | `VID_0403&PID_6014` 为 FTDI USB 设备，Vivado target 仍需单独通过 preflight 证明 |

关键判据：

```text
VIVADO_PREFLIGHT_SKIPPED=1
IS_ADMIN=0
FTDI6014_PRESENT=1
ADMIN_NOTE=Current shell is not elevated; driver re-scan or re-bind operations may require an elevated PowerShell.
FTDI6014_NOTE=Connected VID_0403&PID_6014 is currently an FTDI USB device; Vivado still must enumerate a hw target before any programming.
```

审计结论更新：

- 可以宣称：JTAG/USB 诊断入口更加可重复，能够在不启动 Vivado 的情况下确认 USB/驱动层状态。
- 不能宣称：驱动重扫、驱动重绑定或 JTAG 枚举已经修复。
- 不能宣称：任何新的 2-lane 物理通信实验已经执行。

## 20. 2026-06-26 03:20 增量审计：plan 硬件恢复总入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增的是执行编排工具，不改变工程目标或实现逻辑。

新增证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| plan 硬件恢复总入口 | 已创建并验证失败路径 | `tools/run_plan_hardware_resume_safe.ps1`，SHA256 `76A3E4583BED081C2F29564B6767FAE23AEF64FBB5EDC8016F47348EB1F5BFAE` |
| 当前 JTAG 失败路径 | 正确阻止实物测试 | `reports/plan_hw_resume_safe_20260626_032033.summary.txt`，SHA256 `5526D418026606A1F536344940C1FB4095F3CFF2D3368841E860FACDBF3D7D47` |
| 完成度审计刷新 | 已执行 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `0CE39C8E349EC5513E818100DD1885D88816EFB6569BDD17C2E0D06E70D3295F` |

关键判据：

```text
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
USB_DIAG_EXIT=0
AUDIT_MATCH=PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
```

审计结论更新：

- 可以宣称：JTAG 恢复后的下一步实物验证入口已经固化，且当前失败路径不会误烧写或误驱动 TFDU。
- 不能宣称：2-lane 实物矩阵、真实 PS/PC TCP/DHCP smoke 或当前 2-lane 协议实物恢复已经完成。

## 21. 2026-06-26 03:24 增量审计：JTAG 驱动修复 dry-run 助手

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增脚本只用于 JTAG/USB 驱动恢复路径，不改变通信目标或工程实现。

新增证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| JTAG 驱动修复 dry-run 助手 | 已创建并验证 dry-run | `tools/repair_jtag_drivers_admin.ps1`，SHA256 `5FEEFA926D992CCEF66D4E92465C5F9C5CBB7850A21C705FB82DDD2FAA630024` |
| dry-run 不改系统驱动 | PASS | `reports/jtag_driver_repair_20260626_032337.summary.txt`，SHA256 `FDAD80D9DCAF93B549387C59C86BE22E6F730360B7692DC805B94158E1C5437B` |
| 非管理员 `-Apply` 拦截 | PASS | `reports/jtag_driver_repair_20260626_032344.summary.txt`，SHA256 `5C877C7523A3FBCBE818DD16C2C1ABF39B252E35D14C0B9BB752E3E007117447` |
| 总入口集成驱动 dry-run | PASS as guard | `reports/plan_hw_resume_safe_20260626_032426.summary.txt`，SHA256 `DE643F74CF2E80F68250D115E3AE930EB1BE6B86CE52E9C009E2A456FE0AD81A` |

关键判据：

```text
DRY_RUN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
APPLY_BLOCKED_NOT_ADMIN=1
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
DRIVER_REPAIR_DRY_RUN_EXIT=0
PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
```

审计结论更新：

- 可以宣称：JTAG 驱动恢复路径已有可重复、默认安全的执行入口。
- 不能宣称：驱动已实际重装、设备已重扫、JTAG target 已恢复。
- 不能宣称：任何新的 2-lane 硬件通信验证已经执行。

## 22. 2026-06-26 03:30 增量审计：PS/PC 旁路探测工具

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增工具只用于旁路观测和 HELLO/STATUS smoke 编排，不发送红外 TX payload，不烧写 FPGA，不驱动 TFDU。

新增证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| UART 只读启动探测 | PASS as tool / latest board text inconclusive | `tools/probe_ps_uart_boot_safe.ps1`，SHA256 `E9FD3B76A927E6434B17A12BBB9DB71F38906162E55B7EB66B35ACCB1D27EC42` |
| 最新 UART probe | INCONCLUSIVE_NO_UART_TEXT | `reports/ps_uart_boot_probe_20260626_032758.summary.txt`，SHA256 `6254A379B58A3AB15C792F83202DA5DD7B339CEC16F5359AECC972A8007BA762` |
| board smoke 安全包装器 | PASS as tool | `tools/run_ps_pc_board_smoke_safe.ps1`，SHA256 `C4CD91DD1A3E71AFA8EE686E884DE8F61567DABF044C1A59D128338A89846B94` |
| board smoke 无 IP 阻断 | PASS as guard | `reports/ps_pc_board_smoke_safe_20260626_032930.summary.txt`，SHA256 `5FD4194044D885C7E03955291458EA3B29C33758D07D97853FBD74667ED50EEC` |
| board smoke 静态 fallback dry-run | PASS as dry-run | `reports/ps_pc_board_smoke_safe_20260626_032935.summary.txt`，SHA256 `B05EB318832F72FE734D0C40F5FFD64EEB5E5B6D05EDFD5A36394EB930B64F81` |

关键判据：

```text
UART_PROBE_VERDICT=INCONCLUSIVE_NO_UART_TEXT
NO_UART_WRITE_DONE_BY_THIS_SCRIPT=1
SMOKE_BLOCKED_NO_TARGET_HOST=1
NO_TCP_CONNECT_DONE_BY_THIS_SCRIPT=1
NO_TX_DATA_DONE_BY_THIS_SCRIPT=1
SMOKE_DRY_RUN=1
PLAN_AUDIT_SUMMARY pass=9 waiting_hardware=5 missing_or_unknown=0 total=17
```

审计结论更新：

- 可以宣称：PS/PC 真实板级 smoke 的旁路入口已准备好，并且当前失败路径不会误发红外数据。
- 不能宣称：PS lwIP bridge 已从 SD 或已有镜像启动。
- 不能宣称：真实 TCP/DHCP smoke 已通过。

## 23. 2026-06-26 03:33 增量审计：SD BOOT 镜像一致性

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只操作本地 BOOT 镜像文件，不进行任何实物测试。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| BOOT 审计脚本 | 完成 | `tools/audit_boot_artifacts.py`，SHA256 `B2B790984D9E9085E912765FD7C05957CA4249B0665403C6736BD6253CE73675` |
| PS lwIP bridge BOOT 包 | PASS | `software/_boot/BOOT.BIN`，SHA256 `4C753690E35F5D3ED2F611E3D83602BF4A94CE260E34FBF241F24378BBF7C30D` |
| PS-PS loopback BOOT 包 | 已重建并 PASS | `software/_boot_ps_ps_loopback/BOOT.BIN`，SHA256 `986AA672F467C5461596AD47303C1A2314C48DAAE10A56C3B5DF9FD51946CBA7` |
| BOOT 审计报告 | PASS=2 / WARN=0 / FAIL=0 | `reports/boot_artifact_audit_current_20260626.md`，SHA256 `1BC1F3A457EEC67AC1E57B62C0C52CA595026AA38EE9D6C5AAB64872A4B6528A` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `6C3304D3D1947AC9F1F835E6FE3FE6292B65C88492961E00A603294551E275D0` |

关键判据：

```text
BOOT_AUDIT_SUMMARY pass=2 warn=0 fail=0 total=2
BOOT-AUDIT=PASS
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
```

审计结论更新：

- 可以宣称：两套 SD BOOT 镜像均具备组件哈希、输入新鲜度和应用 marker 审计证据。
- 不能宣称：任意 SD 镜像已经在开发板上成功启动。

## 24. 2026-06-26 03:35 增量审计：静态 fallback TCP smoke 真实失败

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只尝试 PC 到 `192.168.1.10:5001` 的 HELLO/STATUS smoke，不发送红外数据。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| board smoke wrapper 判定修复 | 完成 | `tools/run_ps_pc_board_smoke_safe.ps1`，SHA256 `618B1AA83350D95C446163367EF46427D96BB4A77E10FBA220434CBFFD0E9C23` |
| 静态 fallback TCP smoke | FAIL_OR_NO_BOARD_RESPONSE | `reports/ps_pc_board_smoke_safe_20260626_033535.summary.txt`，SHA256 `CCA85400C1B6EFD6E6C01CE08F740E661340297AF97CCCE7579569BD815749CD` |
| 网络邻居状态 | Unreachable | `192.168.1.10` on `以太网`, link-layer `00-00-00-00-00-00` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `81A4AB88BDDBFE77C515D7FFB1A4C17925719B6F354C39B9820A0F3AB77CCE35` |

关键判据：

```text
SMOKE_STDERR_FAILURE_PARSED=1
SMOKE_RESPONSE_OK_PARSED=0
SMOKE_VERDICT=FAIL_OR_NO_BOARD_RESPONSE
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
P2-BOARD=WAITING_HARDWARE
```

审计结论更新：

- 可以宣称：当前真实板级 TCP smoke 已尝试且失败原因是目标无响应/连接超时。
- 可以宣称：wrapper 不再将连接超时误判为通过。
- 不能宣称：真实 AX7010 PS-to-PC TCP/DHCP smoke 已通过。

## 25. 2026-06-26 03:39 增量审计：smoke 有效退出码与最新重试

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮没有烧写 FPGA，没有发送红外 payload，没有驱动 TFDU；只重试静态 fallback IP 的 HELLO/STATUS smoke。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| host acceptance wrapper 失败退出 | 完成 | `software/host_client/run_acceptance.ps1`，SHA256 `C0C9AB13CA9FF9DAED9433ACD735DCADBDAC412F77E36F27C22FBECBD18F0CE3` |
| board smoke wrapper 有效退出码 | 完成 | `tools/run_ps_pc_board_smoke_safe.ps1`，SHA256 `3B7885B9B1FFF70682098EF054D41FC4402E1621BBB83C479E3B92243CC2AAD7` |
| 最新静态 fallback TCP smoke | FAIL_OR_NO_BOARD_RESPONSE | `reports/ps_pc_board_smoke_safe_20260626_033931.summary.txt`，SHA256 `9B7554D2CCDFF838AC74497EBAACDB792A6810905AE1EB653851422CCCABF40F` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `F712BA720306859B7B401B636301D190D7915B45CBEB4BB748C2095ACB5DF030` |

关键判据：

```text
SMOKE_EXIT=0
SMOKE_STDERR_FAILURE_PARSED=1
SMOKE_RESPONSE_OK_PARSED=0
SMOKE_VERDICT=FAIL_OR_NO_BOARD_RESPONSE
SMOKE_EFFECTIVE_EXIT=1
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
P2-SMOKE-TOOL=PASS
P2-BOARD=WAITING_HARDWARE
```

审计结论更新：

- 可以宣称：板级 smoke 工具自身通过审计，且失败路径不会再被误标为 PASS。
- 不能宣称：真实 AX7010 PS-to-PC TCP/DHCP smoke 已通过。
- 当前 P2-BOARD 继续保持 `WAITING_HARDWARE`，等待 UART 出现 `Board IP` 或 JTAG 恢复后再验证。

## 26. 2026-06-26 03:42 增量审计：最新离线门禁与 plan 条目级状态

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只运行离线/host 工具和审计脚本，没有进行任何实物发光、烧写或 JTAG 操作。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| 最新 PS/PC 离线门禁 | PASS | `reports/ps_pc_offline_gates_20260626_034206.summary.txt`，SHA256 `DED70DF9F1C3423A276FF97632D32A51D03ABB0069578B2BD54188B420B6968A` |
| plan 条目级审计脚本 | 完成 | `tools/audit_plan_items.py`，SHA256 `B5E580B0AE8A2081CF31C66AF05E2FAC4B4F7BB46F0C56429A2C2BE0053B68AC` |
| plan 条目级审计报告 | 已生成 | `reports/plan_item_audit_current_20260626.md`，SHA256 `E707B3B4091E73DC53E2951447AD37D0397C4FAD72348C5475269E2B5A68F16B` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `1122C9C2EA0FBA0E501428FBC7C56DD9A995B633E19275B1418B483355C2F9F0` |

关键判据：

```text
PS_PC_OFFLINE_GATES_PASS static=1 unittest=1 offline_mock=1
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
```

条目级审计判读：

- `P0-1` 与 `M0` 已通过：证据冻结和配置 diff 已存在。
- `P0-4` 与 `M3` 是 `PARTIAL_SIM_PASS`：仿真有 session/mask/RX microscope 支撑，但实物失败分类尚未完成。
- `P0-5` 与 `P1-1` 是 `READY_FOR_HARDWARE`：当前 ILA/RX microscope build 和 lane mapping 安全工具已准备，但等待 JTAG/硬件证据。
- `P0-2`、`P0-3`、`P0-6`、`P1-2`、`P2-2`、`M1` 到 `M7` 的实物闭环项仍为 `WAITING_HARDWARE`。
- `P3`、`M8`、`M9`、`M10` 仍未开始，必须等待 2-lane 当前版本恢复、PC-to-PC smoke 和 soak 前置条件。

## 27. 2026-06-26 03:47 增量审计：JTAG 最新失败路径归档

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只进行 JTAG 枚举预检、USB 诊断和驱动 dry-run；没有烧写 FPGA，没有进入 2-lane matrix，没有驱动 TFDU。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| JTAG 只枚举预检 | FAIL_NO_TARGET | `reports/hw_target_preflight_20260626_034713.summary.txt`，SHA256 `E75AE2F5DFB0D830BE81471646FE29D124A58734D24F59E4138F0AB70D2966B5` |
| 集成恢复入口失败路径 | 安全阻断 | `reports/plan_hw_resume_safe_20260626_034713.summary.txt`，SHA256 `65567560D79BB256B00ECDFBB1EA38F6F21E0AE50E903BB7C254D21EAF6F7BC2` |
| resume-safe 有效退出码记录 | 已补强 | `tools/run_plan_hardware_resume_safe.ps1`，SHA256 `64F58D7125EBDBD05B49599B02A922245E7F7A523028F387951D350E63DDCE0F` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `D2722A12E575252399C09BEC88B662D252F62175B95021455821AE4341F934DD` |
| 条目级审计 | 已刷新 | `reports/plan_item_audit_current_20260626.md`，SHA256 `86E36B2B990B71FCBD640D221FF50486C392EE9D9D579AB4E7856468E2CB6411` |

关键判据：

```text
COM_PORT_PRESENT=1
FTDI6014_PRESENT=1
VIVADO_PREFLIGHT_EXIT=2
HW_PREFLIGHT_TARGET_COUNT 0
HW_PREFLIGHT_RESULT FAIL_NO_TARGET
PREFLIGHT_EFFECTIVE_EXIT=2
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
```

审计结论更新：

- 可以宣称：最新硬件恢复尝试被正确挡在 JTAG target 枚举失败之前，未进入任何烧写或 TFDU 运行阶段。
- 可以宣称：当前 PC 能看到 FTDI6014 与 CP2102，但 Vivado 仍枚举不到 Zynq target。
- 不能宣称：known-good 单 lane 或 2-lane 硬件回放已执行。

## 28. 2026-06-26 03:50 增量审计：JTAG 绑定诊断增强

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只运行 USB/JTAG 只读诊断，没有修改系统驱动，没有烧写 FPGA，没有驱动 TFDU。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| USB/JTAG 诊断脚本 | 已增强 | `tools/diagnose_jtag_usb.ps1`，SHA256 `4C0C99F9B797CD03C877C7289A8E1984D9F5A15D1521CB6BB99FCFE2CA2742FC` |
| 最新 USB/JTAG 诊断 | FTDI visible / no Vivado target | `reports/jtag_usb_diag_20260626_035039.summary.txt`，SHA256 `D07FE1BF67BCE34C82CC579B8FF79EF447E22E8BEBF95CDB61152E0E06F90FFF` |
| FTDI6014 驱动详情 | ftdibus.inf / FTDI | `reports/jtag_usb_diag_20260626_035039.ftdi6014_USB_VID_0403_PID_6014_210512180081.drivers.log`，SHA256 `C5F0ED0CB28DAAE0056573610B820993A32E2FAACD0DC327BC460338E7DDDF37` |
| 总完成度审计 | 已刷新 | `reports/plan_completion_audit_current_20260626.md`，SHA256 `15377508D2016E0AFBD261BF38665D1AAE6D9FC5ABE9B11E78E7F3282B9BC991` |
| 条目级审计 | 已刷新 | `reports/plan_item_audit_current_20260626.md`，SHA256 `49545A3EA0E85E4DB60106BD6F1607584C22FF1D7302A3119D8E8896C31613EE` |

关键判据：

```text
FTDI6014_BOUND_SERVICE=FTDIBUS
FTDI6014_BOUND_TO_FTDIBUS=1
FTDI6014_DRIVER_PROVIDER_FTDI=1
FTDI6014_DRIVER_ORIGINAL_FTDIBUS=1
FTDI6014_DRIVER_ORIGINAL_WINUSB=0
PNPUTIL_CONNECTED_MATCH pattern=Digilent count=0
PNPUTIL_CONNECTED_MATCH pattern=Xilinx count=0
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
```

审计结论更新：

- 可以宣称：当前 USB 层看到的是 FTDI6014 绑定 FTDI `ftdibus.inf`，这解释了“USB 设备可见但 Vivado target count 为 0”的一个高优先级排查方向。
- 不能宣称：JTAG 已恢复或 Vivado 已能枚举 Zynq。
- `P0-2`、`P0-3`、`M1`、`M2` 等硬件回放项仍保持 `WAITING_HARDWARE`。

## 29. 2026-06-26 03:53 增量审计：JTAG 恢复后自动接续验证入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮新增 JTAG 恢复包装脚本并验证 dry-run / 非管理员阻断路径；没有修改系统驱动，没有烧写 FPGA，没有驱动 TFDU。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| JTAG 恢复后接续验证脚本 | 已创建 | `tools/run_jtag_driver_recovery_then_resume.ps1`，SHA256 `6641B0C631D059AEB5C5716ED3FB86FAC04D5C0826DC0448AA41A518D0DE188A` |
| dry-run 自检 | PASS as dry-run | `reports/jtag_recovery_then_resume_20260626_035316.summary.txt`，SHA256 `2A69A5B26E9BA8EB6FD6DED4146AF5CE9ED3EF9E8117B4EBEF72EE3F283D407B` |
| 非管理员 Apply 阻断 | PASS as guard | `reports/jtag_recovery_then_resume_20260626_035326.summary.txt`，SHA256 `1CD8DC3E4D18F18C3A4FA49B0FBDF26239ADC1443331E610FD7CBFE09E71B1BF` |

关键判据：

```text
DRY_RUN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
NO_RESUME_RUN_DONE=1
TO_RUN_ELEVATED=powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_jtag_driver_recovery_then_resume.ps1 -Apply -InstallPcUsb -InstallDigilent -StopOnFail
APPLY_BLOCKED_NOT_ADMIN=1
```

审计结论更新：

- 可以宣称：管理员修复驱动后自动接续 `run_plan_hardware_resume_safe.ps1` 的入口已经准备好，且默认不改系统。
- 不能宣称：驱动已实际修复或 JTAG target 已恢复。

## 30. 2026-06-26 03:57 增量审计：P0 known-good 回放入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只新增/加固脚本并验证 preflight 阻断路径；没有烧写 FPGA，没有驱动 TFDU。

新增/更新证据：

| 项 | 当前状态 | 证据 |
|---|---|---|
| lane0 单次 runner preflight 加固 | 完成 | `tools/run_lane0_hw_once_safe.ps1`，SHA256 `F14A0AF00B1C9EB98F6009C0759C7B81ABFF8B4E9EE3D7FC23D5AB6BB93F2A7B` |
| P0 known-good replay wrapper | 已创建 | `tools/run_p0_known_good_replay_safe.ps1`，SHA256 `41751DEB604F7199296ADF056CDA272D4721B20A17C11230D83ED50EBF56BD17` |
| P0 wrapper dry-run | PASS as dry-run | `reports/p0_known_good_replay_safe_20260626_035632.summary.txt`，SHA256 `7685B048708D5A6B315AAB817D6360D78AC325A15B60E182502A3FD039C2361B` |
| lane0 runner JTAG 阻断 | PASS as guard | `reports/lane0_hw_loopback_safe_20260626_035717.summary.txt`，SHA256 `3D0A43F187CB7EBE77A7B87231F091DA38B6F3463BC8B6F77C9E0E125A43A922` |
| P0 wrapper JTAG 阻断 | PASS as guard | `reports/p0_known_good_replay_safe_20260626_035743.summary.txt`，SHA256 `A1B9B02CC974CD7C02FDBAE11FC89A109C3A48B7FF3B3E0B1973A13D8DA31B0E` |
| 条目级审计 | 已刷新 | `reports/plan_item_audit_current_20260626.md`，SHA256 `B501CDC0970E3AC83B9139F5224512C463AD9CDE97B59C76C03ED8B40A4BE5D1` |

关键判据：

```text
LANE0_RECIPE=IR_LANE_COUNT=1,CNT_CHIP_MAX=7,lane_mask=0x1,session=0x2201,payload_bytes=247
TWO_LANE_RECIPE=IR_LANE_COUNT=2,B_SESSION_ID=0x2203,lane_mask=0x3,trigger=b_tx_nonzero
PREFLIGHT_EFFECTIVE_EXIT=2
PREFLIGHT_PASS_PARSED=0
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
P0_REPLAY_BLOCKED_NO_PROGRAMMING=1
```

审计结论更新：

- 可以宣称：`P0-2` 和 `P0-3` 已有安全回放入口，且该入口当前正确阻断于 JTAG target 不可见。
- 不能宣称：known-good lane0 或 known-good 2-lane 已在当前硬件上重新通过。
