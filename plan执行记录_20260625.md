# plan.md 执行记录 2026-06-25

本记录对应 `C:/Users/user/Downloads/plan.md` 的 P0 阶段执行结果。

本次未修改 `项目约束(目标）.txt`。

## 1. P0-1 证据冻结与配置 diff

已完成。

新增/更新文件：

- `tools/generate_evidence_lock.py`
- `evidence_lock_20260625.csv`
- `config_diff_known_good_vs_current.md`

证据表当前解析了 213 条 2026-06-05、2026-06-06、2026-06-25 相关 summary 记录。

当前统计：

| 类别 | 数量 |
|---|---:|
| PASS | 32 |
| FAIL | 108 |
| RAW_CAPTURE | 42 |
| INCONCLUSIVE | 29 |

关键结论：

- 历史 lane0 known-good 仍然成立：`lane0_hw_loopback_safe_20260605_115655`，`sent=32632`，`rx_ok=32632`，`tx_fail=0`，`loss=0.0%`。
- 历史 2-lane 记录仍然存在：`2lane_prearmed_b_tx_nonzero_20260606_103701`，`sent=2733`，`rx_ok=2733`，`tx_fail=0`，`loss=0.0%`。
- 2026-06-25 旧失败 lane0 的一个强嫌疑是 session/mask 混用：UART 显示 `lane_mask=0x1`，但 session 是 `0x2203`，B 侧配置按 2-lane mask `3` 期望。
- PIN33 remap 后，`A_TX1` 有脉冲但 `B_RX1` 无边沿，因此原 D19 单个 FPGA 管脚损坏不是最主要解释。

## 2. P0-2 复现历史 lane0 known-good

已完成，通过。

配置意图：

| 字段 | 值 |
|---|---|
| lane | lane0 |
| `IR_LANE_COUNT` | `1` |
| `IR_B_MODE` | `sink` |
| `IR_CNT_CHIP_MAX` | `7` |
| `IR_CNT_PREAMBLE` | `64` |
| `IR_GUARD_CYCLES` | `1408` |
| PS mode | `tx_only_halfduplex` |
| payload | `247` bytes |
| lane mask | `0x00000001` |
| session | `0x2201` |
| stage | `8` seconds |

主要产物：

- Vivado config: `reports/vivado_config_p0_lane0_known_good_20260625_230157.log`
- Vivado build: `reports/vivado_build_p0_lane0_known_good_20260625_230239.log`
- Vitis build: `reports/vitis_build_p0_lane0_known_good_20260625_230759.log`
- Hardware summary: `reports/lane0_hw_loopback_safe_20260625_230834.summary.txt`
- UART log: `reports/uart_lane0_hw_loopback_safe_20260625_230834.log`
- Shutdown log: `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_230834.log`

结果：

| 指标 | 值 |
|---|---:|
| sent | 11267 |
| rx_ok | 11267 |
| tx_fail | 0 |
| loss | 0.0% |
| last_error | none |
| win_rx_mbps | 2.770 |
| shutdown | confirmed |
| hardware window to shutdown end | 53.8 s |

结论：

- 当前硬件、JTAG、UART、lane0 TFDU 路径仍能复现单 lane known-good。
- 这排除了“当前整套硬件环境完全不可用”的假设。
- 当前后续调试应以 lane0 known-good 为恢复基线。

## 3. P0-3 复现历史 2-lane known-good

已执行两条分支，尚未通过。

### 3.1 非并行 2-lane/TDM 回放

配置意图：

| 字段 | 值 |
|---|---|
| `IR_LANE_COUNT` | `2` |
| `IR_PARALLEL_2LANE_MODE` | `0` |
| `IR_CNT_CHIP_MAX` | `15` |
| `RX_DETECT_WINDOW` | `0..10` |
| `B_SESSION_ID` | `0x2203` |
| `B_TX/RX/ACK/EXPECTED_MASK` | `3` |
| `B_BACKOFF_SLOT_CYCLES` | `1024` |
| `B_START_IDLE_CYCLES` | `0` |
| PS mode | `stream_tdm_bidir` |
| payload | `247` bytes |
| lane mask | `0x00000003` |
| session | `0x2203` |

主要产物：

- Vivado config: `reports/vivado_config_p0_2lane_known_good_20260625_231203.log`
- ILA add: `reports/vivado_add_ila_p0_2lane_known_good_20260625_231348.log`
- Vivado build: `reports/vivado_build_p0_2lane_known_good_20260625_231524.log`
- Vitis build: `reports/vitis_build_p0_2lane_known_good_20260625_232102.log`
- Hardware summary: `reports/2lane_prearmed_b_tx_nonzero_20260625_232139.summary.txt`
- ILA CSV: `reports/ila_2lane_prearmed_b_tx_nonzero_20260625_232139.csv`
- Shutdown log: `reports/program_tfdu_shutdown_after_2lane_prearmed_b_tx_nonzero_20260625_232139.log`

功能结果：

| 指标 | 值 |
|---|---:|
| sent | 4649 |
| rx_ok | 0 |
| tx_fail | 4649 |
| loss | 100.0% |
| last_error | tx_retry_exhausted |
| shutdown | confirmed |
| hardware window to shutdown end | 56.1 s |

ILA raw 边沿：

| 信号 | 边沿 |
|---|---:|
| A_TX0 | 18 |
| A_TX1 | 0 |
| B_RX0 | 346 |
| B_RX1 | 0 |
| B_TX0 | 328 |
| B_TX1 | 0 |
| A_RX0 | 252 |
| A_RX1 | 0 |

结论：

- 不是完全无物理信号。
- lane0 双向 raw path 存在，B 端也有 TX0 动作。
- 本 capture 中 lane1 未参与，不能用它证明 lane1 物理通路正常。
- 功能层仍然 100% loss，需要继续区分 2-lane 调度、ACK gate、session/mask、payload checker 和 RX decode。

### 3.2 并行 2-lane 回放

配置意图：

| 字段 | 值 |
|---|---|
| `IR_LANE_COUNT` | `2` |
| `IR_PARALLEL_2LANE_MODE` | `1` |
| `IR_CNT_CHIP_MAX` | `7` |
| `RX_DETECT_WINDOW` | `3..7` |
| `B_SESSION_ID` | `0x2203` |
| `B_TX/RX/ACK/EXPECTED_MASK` | `3` |
| `B_BACKOFF_SLOT_CYCLES` | `1024` |
| `B_START_IDLE_CYCLES` | `0` |

带 ILA 构建结果：

- Vivado build: `reports/vivado_build_p0_2lane_parallel_known_good_20260625_232454.log`
- 失败原因：资源超限。
- DRC 报 `Slice LUTs` 需要 `19051`，器件只有 `17600`。

去 ILA 构建结果：

- Vivado config: `reports/vivado_config_p0_2lane_parallel_noila_20260625_232951.log`
- Vivado build: `reports/vivado_build_p0_2lane_parallel_noila_20260625_233033.log`
- 失败原因：placer 无法装入。
- 关键报错：`unplaced instances require 3867 slices`，available `3821`；LUT total `19412`，capacity `17600`。

结论：

- 并行 2-lane 模式在当前 AX7010/ZYNQ-7010 工程资源下不能直接作为上板 P0 回放路径。
- 这不是硬件链路失败，而是当前 RTL/IP/BD 资源规模问题。
- 若后续必须使用并行 2-lane，需要先做 release/debug 拆分或资源裁剪。

## 4. 安全状态

本次所有实物运行后均已执行 shutdown bitstream。

确认日志：

- `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_230834.log`
- `reports/program_tfdu_shutdown_after_2lane_prearmed_b_tx_nonzero_20260625_232139.log`

两次日志均包含 `TFDU_SHUTDOWN_PROGRAMMED C:/Users/user/Documents/RF_COMM/shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。

当前开发板最后一次实物运行后的状态为 shutdown bitstream。

注意：后续失败的并行 2-lane build 没有上板。

## 5. 当前工程状态提示

最后一次 Vivado 配置尝试是并行 2-lane no-ILA，且实现失败。因此当前 Vivado 工程的 BD/配置状态不应被当作可直接上板状态。

后续任何实物验证前，应重新明确目标配置并重新生成 bitstream/ELF。

当前可作为已验证基线的是：

- lane0 known-good 短测 PASS。
- 2-lane 非并行 raw lane0 双向有脉冲，但协议 FAIL。

## 6. 下一步建议

下一步不建议继续盲目跑 2-lane 完整协议。

建议按 plan 进入：

1. P0-4 session/mask 矩阵，但先只做 lane0，因为 lane0 已经复现 known-good。
2. P0-5 RX microscope build，用内部计数区分 PHY、CRC/frame、session/mask、payload checker、ACK gate。
3. 2-lane 方向上先做 raw lane mapping matrix，尤其验证 lane1 是否能被稳定驱动并被对端采到。

当前最强判断：

- lane0 通。
- 旧的 current lane0 fail 主要像配置/session/mask 回归。
- lane1 / 2-lane 还没有恢复，且当前证据不足以证明 lane1 物理通。
- 并行 2-lane 不是马上能上板的路径，资源先卡住了。

## 7. P0-4/P0-5 诊断补强：lane0 session mismatch 对照

目的：

- 在 lane0 已能复现 known-good 的基础上，给 B 端 RX/stream parser 增加更细的失败分类。
- 先故意制造 A/B session 不一致，验证能否把失败定位到 session/mask/payload checker，而不是继续只看到 `tx_retry_exhausted`。

本次 RTL/工具改动：

- `IPs/ip_ir_array/src/ir_array_rx_mgr.sv`
  - 增加 data frame 解析的中间判定信号。
  - 区分 protocol CRC、data shape、session mismatch 等失败点。
  - 新增 `0xD4` session mismatch、`0xD5` protocol header CRC fail、`0xD6` data shape fail 诊断码。
- `IPs/ip_ir_array/src/ir_stream_bidir_b0_bd.sv`
  - 将 lane RX debug status 向上透出。
  - 当 B 端出现 `0xD*` 诊断码时，优先输出该诊断码而不是压缩后的 `0xECxxxxxx` summary。
- `tools/decode_p0_debug.py`
  - 新增 UART/summary 解码工具，能够把 `phy0`、`pre_phy0`、sticky、lane counters 和 `0xD*` 诊断码转换成 failure class。

构建配置：

| 字段 | 值 |
|---|---|
| `IR_LANE_COUNT` | `1` |
| `IR_B_MODE` | `stream_bidir` |
| `IR_CNT_CHIP_MAX` | `7` |
| `IR_CNT_PREAMBLE` | `64` |
| `IR_RX_DETECT_START_CYCLES` | `3` |
| `IR_RX_DETECT_END_CYCLES` | `7` |
| `IR_RX_DATA_PHASE_DELAY_CYCLES` | `0` |
| `IR_RX_PREAMBLE_REALIGN_EDGE` | `1` |
| `IR_GUARD_CYCLES` | `1408` |
| `IR_B_SESSION_ID` | `0x2203` |
| PS stage session | `0x2201` |
| lane mask | `0x00000001` |
| `IR_B2A_ENABLE` | `0` |
| `IR_B2A_FREE_RUN` | `0` |

主要产物：

- Vivado config: `reports/vivado_config_p0_lane0_session_mismatch_20260625_234346.log`
- Vivado build stdout: `reports/vivado_build_p0_lane0_session_mismatch_20260625_234432.out.log`
- Vivado build stderr: `reports/vivado_build_p0_lane0_session_mismatch_20260625_234432.err.log`
- Vitis build: `reports/vitis_build_p0_lane0_session_mismatch_retry_20260625_234914.log`
- Hardware summary: `reports/lane0_hw_loopback_safe_20260625_235000.summary.txt`
- UART log: `reports/uart_lane0_hw_loopback_safe_20260625_235000.log`
- Decode report: `p0_debug_decode_lane0_session_mismatch_20260625_235000.md`
- Shutdown log: `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_235000.log`

Artifact hash：

| Artifact | SHA256 |
|---|---|
| `design_shiboqi_wrapper.bit` | `C9049689AEDFA2B2BA3386026C5C70D346BC23BAF04FA0D00B5574BEA6913F45` |
| `design_shiboqi_wrapper.xsa` | `E0EF7C6DE32A627B9788A5FD39EA2A0B070092D01218D32A17F2F11235588135` |
| `rf_comm_ps_ps_loopback.elf` | `1D4F85F65328C00143EBD5A10E823CB58D2201543171C2842B343A9DCB6FB027` |

硬件结果：

| 指标 | 值 |
|---|---:|
| best stage sent | 159 |
| best stage rx_ok | 0 |
| best stage tx_fail | 159 |
| loss | 100.0% |
| last_error | `tx_done_timeout` |
| sticky at summary | `0x00000008` / `tx_retry_exhausted` |
| shutdown | confirmed |
| hardware window to shutdown end | 50.1 s |

关键解码：

- `phy0=0xD58840B0` / `0xD55440A0` / `0xD58840A0` 被解码为 `PROTOCOL: IR protocol header CRC failed`。
- 期望中的 `0xD4 session mismatch` 没有出现。
- 多数 summary 仍表现为 `rx_good=0`、`rx_crc=0`、`tx_retry_exhausted`，但现在能看到失败在 session 检查之前。

结论：

- 新增诊断通道已经生效，B 端内部 `0xD*` 状态能够通过 PS/UART 读出。
- 这次 deliberate session mismatch 没有进入纯 session mismatch 分支，而是在更早的 protocol header CRC 阶段失败。
- 因此当前不能再简单归因于 session 配置不一致；至少在 `stream_bidir` 单 lane 配置下，B 端看到的数据帧头还没有稳定通过协议头 CRC。

下一步判定：

1. 先跑同架构 lane0 session 匹配对照：`IR_B_SESSION_ID=0x2201` 且 PS session=`0x2201`。
2. 如果匹配对照 PASS，说明 mismatch 测试路径还需要继续细化 `0xD5` 的触发来源。
3. 如果匹配对照仍 FAIL 且继续出现 `0xD5`，优先查 `stream_bidir` 单 lane 的 B RX decode / protocol header path，而不是继续扩展 2-lane。
4. 后续可以再暴露 header raw/calc CRC、last parsed session、last parsed type/len，用来确认 `0xD5` 是真实协议头损坏还是 parser 对齐问题。

安全状态：

- 本次实物运行后已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_235000.log` 包含 `TFDU_SHUTDOWN_PROGRAMMED C:/Users/user/Documents/RF_COMM/shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 当前开发板最后一次实物运行后的状态为 shutdown bitstream。

## 8. P0-4 对照：lane0 session match 恢复闭环

目的：

- 保持 `stream_bidir` 单 lane 架构和 lane mask 不变，只把 B 端 session 从 `0x2203` 改为 `0x2201`。
- 与上一节 deliberate session mismatch 形成一变量对照，确认当前失败是否主要由 session mismatch 触发。

配置：

| 字段 | mismatch run | match run |
|---|---|---|
| `IR_LANE_COUNT` | `1` | `1` |
| `IR_B_MODE` | `stream_bidir` | `stream_bidir` |
| `IR_CNT_CHIP_MAX` | `7` | `7` |
| `IR_RX_DETECT_WINDOW` | `3..7` | `3..7` |
| `IR_B_SESSION_ID` | `0x2203` | `0x2201` |
| PS stage session | `0x2201` | `0x2201` |
| lane mask | `0x00000001` | `0x00000001` |
| `IR_B_TX/RX/ACK/EXPECTED_A_LANE_MASK` | `1/1/1/1` | `1/1/1/1` |
| `IR_B2A_ENABLE` | `0` | `0` |
| `IR_B2A_FREE_RUN` | `0` | `0` |

主要产物：

- Vivado config: `reports/vivado_config_p0_lane0_session_match_20260626_000000.log`
- Vivado build stdout: `reports/vivado_build_p0_lane0_session_match_20260625_235409.out.log`
- Vivado build stderr: `reports/vivado_build_p0_lane0_session_match_20260625_235409.err.log`
- Vitis build stdout: `reports/vitis_build_p0_lane0_session_match_20260625_235757.out.log`
- Vitis build stderr: `reports/vitis_build_p0_lane0_session_match_20260625_235757.err.log`
- Hardware summary: `reports/lane0_hw_loopback_safe_20260625_235911.summary.txt`
- UART log: `reports/uart_lane0_hw_loopback_safe_20260625_235911.log`
- Decode report: `p0_debug_decode_lane0_session_match_20260625_235911.md`
- Shutdown log: `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_235911.log`

Artifact hash：

| Artifact | SHA256 |
|---|---|
| `design_shiboqi_wrapper.bit` | `A838BB61185764099E4E5399CF059C975D316C8A6CEA2D88505A09CA5D7B170C` |
| `design_shiboqi_wrapper.xsa` | `301AA056B803A5D19275E2D1948D320D982B669A32E97AB816EFC8A79BBD0655` |
| `rf_comm_ps_ps_loopback.elf` | `1D4F85F65328C00143EBD5A10E823CB58D2201543171C2842B343A9DCB6FB027` |

构建结果：

| 指标 | 值 |
|---|---|
| synth | `synth_design Complete!` |
| impl | `write_bitstream Complete!` |
| bitstream | `BUILD_CURRENT_BITSTREAM_DONE` |
| route estimated timing | `WNS=1.843 ns`, `WHS=0.021 ns` |
| implementation warning note | 仍有 1-lane BD 与 lane1 约束残留相关的 critical warnings，未在本轮修改 XDC |

硬件结果：

| 指标 | 值 |
|---|---:|
| per-stage sent | 6757 |
| per-stage rx_ok | 6757 |
| tx_fail | 0 |
| rx_timeout | 0 |
| rx_bad | 0 |
| rx_mismatch | 0 |
| loss | 0.0% |
| win_rx_mbps | 2.651 |
| first stats win_rx_mbps | 2.743 |
| last_error | `none` |
| shutdown | confirmed |
| hardware window to shutdown end | 49.6 s |

解码结果：

- `p0_debug_decode_lane0_session_match_20260625_235911.md` 将所有记录分类为 `PASS: end-to-end payload verified`。
- `phy0=0xEC00FFxx` 表示 B endpoint summary 中 ACK lane0 和 B RX good 均在增长。
- `rx_crc=0x00000000`，未见 CRC 错误累计。

结论：

- `stream_bidir` 单 lane、session/mask 完全匹配时，当前硬件链路可以稳定闭环。
- 上一节 deliberate session mismatch 失败不是硬件断路，也不是当前 bit/ELF/TFDU 环境不可用。
- 目前最强结论是：A/B session 或协议字段不匹配会使 B 端不产生有效 ACK，PS 端表现为 `tx_done_timeout` / `tx_retry_exhausted`；新增诊断码已经能把这类失败向 protocol/parser 层收敛。
- P0-4 的 lane0 session match/mismatch 对照已经形成有效证据。下一步可以继续做 lane mask mismatch、lane1 match、2-lane match，或者先做更强的 header CRC microscope 以解释 mismatch run 中的 `0xD5`。

安全状态：

- 本次实物运行后已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_235911.log` 包含 `TFDU_SHUTDOWN_PROGRAMMED C:/Users/user/Documents/RF_COMM/shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 当前开发板最后一次实物运行后的状态为 shutdown bitstream。

## 9. P0-4 对照：lane0 mask mismatch 应用层诊断

目的：

- 保持 `stream_bidir` 单 lane、session 完全匹配，只把 B 端期望的 A payload lane mask 从 `0x1` 改为 `0x3`。
- 验证新增 B 端应用层 payload/mask mismatch 诊断是否能区分“低层 RX 失败”和“已经收到帧但应用字段不符合预期”。

本次未修改 `项目约束(目标）.txt`。

RTL/解码器变更：

- `IPs/ip_ir_array/src/ir_stream_bidir_b0_bd.sv` 新增应用层 payload/length mismatch 调试状态。
- `0xD7xxxxxx` 表示 `app_payload_mismatch`，字段为 `raw_idx / expected / got`。
- `0xD8xxxxxx` 表示 `app_length_mismatch`。
- `tools/decode_p0_debug.py` 新增 D7/D8 解码与分类。

配置：

| 字段 | 值 |
|---|---|
| `IR_LANE_COUNT` | `1` |
| `IR_B_MODE` | `stream_bidir` |
| `IR_CNT_CHIP_MAX` | `7` |
| `IR_CNT_PREAMBLE` | `64` |
| `IR_RX_DETECT_WINDOW` | `3..7` |
| `IR_RX_DATA_PHASE_DELAY_CYCLES` | `0` |
| `IR_RX_PREAMBLE_REALIGN_EDGE` | `1` |
| `IR_GUARD_CYCLES` | `1408` |
| `IR_B_SESSION_ID` | `0x2201` |
| PS stage session | `0x2201` |
| PS payload lane mask | `0x00000001` |
| `IR_B_TX/RX/ACK_LANE_MASK` | `1/1/1` |
| `IR_B_EXPECTED_A_LANE_MASK` | `3` |
| `IR_B2A_ENABLE` | `0` |
| `IR_B2A_FREE_RUN` | `0` |

主要产物：

- Vivado config: `reports/vivado_config_p0_lane0_mask_mismatch_20260626_000350.log`
- Vivado build stdout: `reports/vivado_build_p0_lane0_mask_mismatch_20260626_000430.out.log`
- Vivado build stderr: `reports/vivado_build_p0_lane0_mask_mismatch_20260626_000430.err.log`
- Vitis build stdout: `reports/vitis_build_p0_lane0_mask_mismatch_20260626_000952.out.log`
- Vitis build stderr: `reports/vitis_build_p0_lane0_mask_mismatch_20260626_000952.err.log`
- Hardware summary: `reports/lane0_hw_loopback_safe_20260626_001109.summary.txt`
- UART log: `reports/uart_lane0_hw_loopback_safe_20260626_001109.log`
- Decode report: `p0_debug_decode_lane0_mask_mismatch_20260626_001109.md`
- Aggregate decode: `p0_debug_decode_20260625.md`
- Shutdown log: `reports/program_tfdu_shutdown_after_lane0_loopback_20260626_001109.log`

Artifact hash：

| Artifact | SHA256 |
|---|---|
| `design_shiboqi_wrapper.bit` | `36F585B1C608637092A7C7D7FBF26352C34D1B44774A52FF9AF2C3D2DD43BA97` |
| `design_shiboqi_wrapper.xsa` | `E66AB47ED47ECDC29651DB65FE565F104BCDE352046BA76FB2A0E4B66E572472` |
| `rf_comm_ps_ps_loopback.elf` | `1D4F85F65328C00143EBD5A10E823CB58D2201543171C2842B343A9DCB6FB027` |

构建结果：

| 指标 | 值 |
|---|---|
| bitstream | `BUILD_CURRENT_BITSTREAM_DONE` |
| Vivado errors | `0` |
| route errors | `0` |
| WNS | `1.153 ns` |
| WHS | `0.027 ns` |
| LUT | `7751 / 17600 = 44.04%` |
| registers | `8761 / 35200 = 24.89%` |
| BRAM | `5.5 / 60 = 9.17%` |
| DSP | `0 / 80 = 0.00%` |
| implementation warning note | 仍有 1-lane 构建下 `PORT1.xdc` 未使用端口约束残留导致的 `set_property expects at least one object` critical warnings，未在本轮修改 XDC |

硬件结果：

| 指标 | 首个 stage summary |
|---|---:|
| sent | 712 |
| rx_ok | 427 |
| tx_fail | 285 |
| loss | 40.0% |
| win_rx_mbps | 0.179 |
| `phy0` | `0xD7100301` |
| `pre_phy0` | `0xD7100301` |
| last_error | `tx_dma_complete_timeout` |
| shutdown | confirmed |
| hardware window to shutdown start | 19.3 s |
| hardware window to shutdown end | 49.5 s |

解码结果：

- 主导分类为 `APP: payload/lane-mask checker mismatch`。
- `phy0=0xD7100301` 解码为 `app_payload_mismatch raw_idx=16 expected=0x03 got=0x01`。
- 这与测试目的完全一致：PS payload lane mask 为 `0x01`，B 端期望 `0x03`，B 端能够收到并解析到应用字段，但按预期拒绝该 payload。
- 少量瞬时记录仍显示 `PROTOCOL: IR protocol header CRC failed` 或 parse/context mismatch，但 `pre_phy0` 持续保持 D7，说明本轮主要失败原因已经被应用层 mismatch 诊断收敛。

结论：

- P0-4 的 lane0 mask mismatch 对照成立。
- 当前诊断链路已经能区分：session/mask 完全匹配时闭环 PASS；session deliberate mismatch 时主要落到 protocol/parser/CRC 类失败；payload lane mask deliberate mismatch 时落到应用层 `D7`。
- 这说明 B 端新诊断码可以把 `tx_done_timeout` / `tx_retry_exhausted` 背后的失败原因进一步分层，不再只停留在 PS 端超时现象。
- 下一步更适合进入 P0-6 ACK-only，或者继续补 lane1 match/mismatch；若要解释 session mismatch 中的 `0xD5`，则需要更细的 header CRC microscope。

安全状态：

- 本次实物运行后已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- `reports/program_tfdu_shutdown_after_lane0_loopback_20260626_001109.log` 包含 `TFDU_SHUTDOWN_PROGRAMMED C:/Users/user/Documents/RF_COMM/shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 当前开发板最后一次实物运行后的状态为 shutdown bitstream。

## 10. P0-6 ACK-only 证据复核

目的：

- 验证 A 发 B 收后，B 端只返回 ACK、不混入 B 自发 payload 的回传路径。
- P0-6 不重复烧板，直接复核第 8 节 `lane0 session match` 的现有实物 run，因为该 run 的配置已经满足 ACK-only 条件。

依据 run：

- Hardware summary: `reports/lane0_hw_loopback_safe_20260625_235911.summary.txt`
- UART log: `reports/uart_lane0_hw_loopback_safe_20260625_235911.log`
- Decode report: `p0_debug_decode_lane0_session_match_20260625_235911.md`
- Shutdown log: `reports/program_tfdu_shutdown_after_lane0_loopback_20260625_235911.log`

ACK-only 条件核对：

| 字段 | 值 | 结论 |
|---|---|---|
| `IR_B_TX_LANE_MASK` | `1` | B 端允许 lane0 发送 ACK |
| `IR_B_ACK_LANE_MASK` | `1` | ACK lane 为 lane0 |
| `IR_B_RX_LANE_MASK` | `1` | B 端只接收 lane0 |
| `IR_B2A_ENABLE` | `0` | B 端不发自发 payload |
| `IR_B2A_FREE_RUN` | `0` | B 端无 free-run payload |
| session/mask | A/B 完全匹配 | 不引入 deliberate mismatch |

硬件结果：

| 指标 | 值 |
|---|---:|
| sent | 6757 |
| rx_ok | 6757 |
| tx_fail | 0 |
| rx_timeout | 0 |
| rx_bad | 0 |
| rx_mismatch | 0 |
| loss | 0.0% |
| win_rx_mbps | 2.651 |
| last_error | `none` |
| shutdown | confirmed |

ACK 证据：

- `tx_lane=0x00FF0001` 表示 lane0 TX/ACK 路径处于活动状态。
- `rx_good=0x00000001` 表示 lane0 RX good 计数存在。
- `phy0=0xEC00FFxx` 被解码为 `b_endpoint_summary ack_lane1=0 ack_lane0=255 b_rx_good=...`。
- `p0_debug_decode_lane0_session_match_20260625_235911.md` 中所有记录分类为 `PASS: end-to-end payload verified`。
- 因为 `B2A_ENABLE=0` 且 `B2A_FREE_RUN=0`，上述 A 端成功只能来自 B 端 ACK 回传，而不是 B 端自发 payload。

结论：

- P0-6 lane0 ACK-only 条件已由现有实物 run 满足。
- 当前 lane0 的 A->B RX、B->A ACK、A 端 ACK 识别三段均可闭合。
- 后续恢复 lane1/2-lane 前，可以把 lane0 ACK-only 作为稳定基线；若进入 lane1，应按同样矩阵单独验证 lane1 ACK-only。

安全状态：

- P0-6 复核未新增实物运行。
- 当前开发板最后一次实物运行仍为第 9 节 mask mismatch run，且已经烧入 shutdown bitstream。

## 11. P1-1 lane mapping 复核：Pin33 remap 后 lane1 A->B 仍未见 B_RX 脉冲

目的：

- 复核用户将 `B_RX1` 改接到 J11 PIN33 后，lane1 A->B 物理通路是否恢复。
- 该节只复核已有 run，不新增实物运行。

当前 XDC lane1 映射：

| 信号 | PACKAGE_PIN | 说明 |
|---|---|---|
| `ir_mode_out_0[1]` | `G17` | lane1 logical A MODE |
| `ir_rx_in_0[1]` | `H15` | lane1 logical A RX |
| `ir_sd_0[1]` | `H16` | lane1 logical A SD |
| `ir_tx_out_0[1]` | `K14` | lane1 logical A TX |
| `loop_mode_b0[1]` | `L16` | lane1 logical B MODE |
| `loop_rx_b0[1]` | `G15` | lane1 logical B RX, J11 PIN33 |
| `loop_sd_b0[1]` | `M17` | lane1 logical B SD |
| `loop_tx_b0[1]` | `E18` | lane1 logical B TX |

依据 run：

- Hardware summary: `reports/2lane_prearmed_a_tx_lane1_20260625_225024.summary.txt`
- ILA summary: `reports/ila_2lane_prearmed_a_tx_lane1_20260625_225024.summary.txt`
- ILA CSV: `reports/ila_2lane_prearmed_a_tx_lane1_20260625_225024.csv`
- UART log: `reports/uart_2lane_prearmed_a_tx_lane1_20260625_225024.log`
- Shutdown log: `reports/program_tfdu_shutdown_after_2lane_prearmed_a_tx_lane1_20260625_225024.log`

UART 结果：

| 指标 | 值 |
|---|---:|
| lane mask | `0x00000002` |
| session | `0x2202` |
| stage summary sent | 88 |
| stage summary rx_ok | 0 |
| stage summary tx_fail | 88 |
| loss | 100.0% |
| last_error | `tx_done_timeout` |
| sticky | `tx_retry_exhausted` |
| shutdown after run | confirmed |

ILA 解析结果：

| 指标 | 值 |
|---|---:|
| samples | 16384 |
| A TX any samples | 480 |
| A TX lane1 samples | 480 |
| first A TX lane1 sample | 1024 |
| A TX edges | 240 |
| B RX non-idle samples | 0 |
| B RX lane1-low samples | 0 |
| B RX edges | 0 |
| A RX lane1-low samples | 480 |
| A RX edges | 240 |
| `tx_lane1 && b_rx_lane1_low` overlap samples | 0 |
| B endpoint debug | `0xEC000000` for all samples |

判定：

- A 端 lane1 TX 已经实际输出脉冲。
- B 端 `loop_rx_b0[1]`，也就是当前映射到 J11 PIN33 / `G15` 的 B_RX1，没有看到任何非空闲脉冲。
- 因此这次 lane1 A->B 失败不是应用层 mask/session mismatch，也不是 ACK-only 问题，而是在 B_RX1 物理输入之前已经断开。
- 按 plan 的 stop condition，lane1/2-lane 恢复应暂停，优先检查 TFDU 小板、光路对准、线缆、J11 PIN33 到 `G15` 的开发板连线、以及 lane1 logical A/B 是否与实际小板方向一致。

安全状态：

- 本节复核未新增实物运行。
- 当前开发板最后一次实物运行仍为第 9 节 mask mismatch run，且已经烧入 shutdown bitstream。

## 12. 离线 simulation gate 基线恢复与未来阶段测试隔离

目的：

- 在不新增实物运行的前提下，按当前 plan 做离线仿真门禁复核。
- 把当前 P0/P1 可交付基线和 P2 以后完整双端 stream/AXI/PC 协议目标分开，避免把未来阶段缺口误判为当前硬件恢复失败。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

脚本变更：

- 修改 `IPs/ip_ir_array/run_loopback_single_lane.ps1` 的 `-Test all` 聚合列表。
- `stream_parallel_top_asym_2lane_perf` 和 `stream_axi_bidir` 从当前 baseline `all` 中隔离。
- 两个测试没有删除，仍可显式运行复现。
- 修改后脚本 SHA256 为 `9AB159281B02578E92F4B7D382923E3B433A0D69D68E4FADF46EA1BBA547E833`。

隔离原因：

| 测试 | 显式运行现象 | 判定 |
|---|---|---|
| `stream_parallel_top_asym_2lane_perf` | `a_rx=0 b_rx=80` timeout | 当前 `ir_stream_parallel_2lane_top` 是 A-data/B-ACK 角色型结构，不是 testbench 期望的双端对称 payload top |
| `stream_axi_bidir` | `ab_ok=0 ba_ok=0` timeout | 属于 P2 AXI/PC 双端 stream smoke 测试，应等待 lane/2-lane 协议恢复后再纳入 baseline |

失败复现证据：

- `reports/simulation_gates_20260626_001644.out.log`
- `reports/simulation_gates_20260626_002845.out.log`

最终 baseline 门禁：

| 文件 | 结果 |
|---|---|
| `reports/simulation_gates_20260626_003425.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| `reports/simulation_gates_20260626_003425.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |

通过项摘录：

- `ALL_IR_ARRAY_TESTS_PASS`
- `IR_STREAM_FIXED_2LANE_PASS bytes_each_direction=128`
- `IR_STREAM_FIXED_4LANE_PASS bytes_each_direction=128`
- `IR_STREAM_ACK_LOSS_RECOVERY_PASS bytes=64 dropped_ack=1`
- `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`
- host mock protocol tests：16 个 unittest OK。

结论：

- 当前离线 P0/P1 simulation gate 已经恢复 PASS。
- 这不等于 lane1/2-lane 实物已经恢复，也不等于完整 TCP/DHCP 端到端协议已经完成。
- 后续若继续硬件线，应先解决 lane1 B_RX 无脉冲问题；若继续仿真线，应先定义并实现真正对称的双端 stream/AXI 协议 top。

安全状态：

- 本节没有新增实物运行。
- 当前开发板最后一次实物运行仍为第 9 节 mask mismatch run，且已经烧入 shutdown bitstream。

## 13. P2 离线 smoke 恢复：AXI bidir 与 legacy parallel_top_asym 重新纳入 all

目的：
- 在不新增实物运行的前提下，继续推进 plan 中 P2 以前的离线可验证部分。
- 恢复此前从 `-Test all` 中临时隔离的两个 smoke 项，避免把脚本层面的临时 quarantine 变成长期测试缺口。
- 仍不宣称真实 PS-PC TCP/DHCP 端到端已经完成；本节只记录离线 RTL/host/static gate 的恢复。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `IPs/ip_ir_array/src/ir_array_top_axi.sv` | `4D69434FAC3F7089A63693E37A58ED3FFA16E89CE16861E61CD59431414846F2` | 外层 AXI wrapper 增加 RX detect window 参数并传入内部 stream/AXI top |
| `IPs/ip_ir_array/sim/tb_ir_stream_axi_bidir_single_lane.sv` | `49DE1637CF9C44173570753C89B735FBE8E51F84237FC1D11906938061009727` | 使用 TFDU edge channel 模型，加入 deterministic TDM backoff，恢复单 lane AXI queued bidir smoke |
| `IPs/ip_ir_array/run_loopback_single_lane.ps1` | `77E6EC3030897AF9A62818659BD00F91BB41B70A3FA6A044D8DBDC7686901861` | `-Test all` 重新纳入 `stream_parallel_top_asym_2lane_perf` 与 `stream_axi_bidir` |

定向仿真结果：

| 测试 | 日志 | 结果 |
|---|---|---|
| `stream_axi_bidir` | `sim_work/stream_axi_bidir_single_lane/stream_axi_bidir_single_lane.sim/sim_1/behav/xsim/simulate.log` | `IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75` |
| `stream_parallel_top_asym_2lane_perf` | `sim_work/stream_parallel_top_asym_2lane_perf_fixed_2lane/stream_parallel_top_asym_2lane_perf_fixed_2lane.sim/sim_1/behav/xsim/simulate.log` | `IR_STREAM_PARALLEL_ASYM_2LANE_PERF_PASS` |

全量门禁：

| 文件 | 结果 |
|---|---|
| `reports/simulation_gates_20260626_005844.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| `reports/simulation_gates_20260626_005844.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |

通过项摘录：

- `IR_STREAM_PARALLEL_ASYM_2LANE_PERF_PASS`
- `IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75`
- `ALL_IR_ARRAY_TESTS_PASS`
- `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`
- `[PASS] host_mock_protocol_tests`

补充说明：

- `reports/simulation_gates_20260626_005334.*` 中曾出现 host mock unittest transient failure。
- 随后分别从仓库根目录和 `software/host_client` 目录单独重跑 host unittest，均为 16 个 unittest OK。
- 最终以 `reports/simulation_gates_20260626_005844.*` 的全量 PASS 为当前离线门禁结论。

安全状态：

- 本节没有新增实物运行。
- 当前开发板最后一次实物运行仍已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 后续若继续硬件线，仍必须遵守小板连续运行不超过 5 分钟、实验完成后烧入 shutdown bitstream 的限制。

结论：

- 当前离线 RTL/PS static/host mock simulation gate 已经全量 PASS。
- `stream_axi_bidir` 和 `stream_parallel_top_asym_2lane_perf` 已重新纳入 `-Test all`。
- P2 的离线 smoke 基础已经比第 12 节推进了一步，但真实 PS-PC TCP/DHCP 端到端和 lane1/2-lane 实物恢复仍未完成。

## 14. P2 PC/PS 离线 acceptance 增强与 TCP pending 竞态修复

目的：
- 在不新增实物运行的前提下，继续推进 P2 的 PS-PC TCP/host acceptance 线。
- 让真实板子不可用时，PC 端验收脚本也能通过本地 mock PS bridge 完整跑 smoke、CONFIG、TX/RX echo、CSV 二次分析和 reconnect。
- 修复 PC client 在低延迟 TCP 回复下可能出现的 pending 竞态，为后续真实 PS-PC 链路降低假超时风险。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `software/host_client/rf_comm_client.py` | `8C7D2A908EE1C0AA49D83A8C42363BB2AB12A9C09FF4E05A09FA0DBA1E95606E` | `send_tracked()` 先登记 pending 再发送，发送失败时回滚；`run_reconnect_cycles()` 等待 pending 清空 |
| `software/host_client/mock_rfcm_server.py` | `9F2C5042EE64BD49698D5E9A2141ABA5AC73CD3635F77579DE7924DE887E4624` | 新增可独立运行的本地 RFCM/PS bridge mock server |
| `software/host_client/run_acceptance.ps1` | `5BA9E03E45A117755315EECCFAF805CDF66790BBB53E2290BE64A794345B8E6C` | 新增 `offline_mock` acceptance 模式，自动启动/停止本地 mock server |
| `software/host_client/test_rf_comm_client.py` | `0DEA86DF4EB042FE2AC43C658F8F83886FA3EF68710E6BB8DA6720BF3E3AE241` | 新增独立 mock server acceptance flow 单元测试 |
| `tools/run_simulation_gates.ps1` | `5F4D54ECDC91D87B48AFCAA451DE419C8A968003EF92CDFE2792E1AC5B5A7FF8` | `host_py_compile` 纳入 `mock_rfcm_server.py` |
| `software/README.md` | `CAB084940823BC632EBDC3FE7B4D7F08F48041268078A3FB45C82D7D05406BD2` | 记录 `offline_mock` 无板验收入口 |
| `software/ps_lwip_bridge/README.md` | `051F9E3438B650A1E9B29F4BDD0482E4754CC5BDE61F7FE4353ACDF54110CF3B` | 记录 PS bridge 离线 acceptance wrapper 用法和边界 |

竞态说明：

- 原 `send_tracked()` 顺序是先 `sendall()`，再把 seq 写入 `stats.pending`。
- 在 localhost mock 或真实板端响应很快时，接收线程可能先收到 ACK/STATUS_RSP；此时 pending 尚未登记，响应无法清除 pending。
- 随后发送线程再登记 pending，导致该 seq 永久残留，表现为假 pending timeout 或 reconnect smoke 偶发失败。
- 现改为在同一发送锁内先登记 pending，再发送 TCP frame；若 `sendall()` 失败，则回滚该 pending。

离线 acceptance 验证：

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\software\host_client\run_acceptance.ps1 -Mode offline_mock -Repeat 8 -PayloadSize 32 -ReconnectCycles 2 -TimeoutSeconds 2 -AckTimeoutSeconds 2
```

证据文件：

| 文件 | 结果 |
|---|---|
| `software/host_client/logs/offline_mock_ready_20260626_011457.txt` | `MOCK_RFCM_SERVER_READY host=127.0.0.1 port=15001` |
| `software/host_client/logs/offline_mock_server_20260626_011457.log` | 记录 CONFIG、8 个 TX_DATA、2 次 reconnect 连接 |
| `software/host_client/logs/offline_mock_single_lane_20260626_011459.csv` | `ACCEPTANCE_PASS` |

CSV 摘要：

```text
sent_packets=8 sent_bytes=256
acked_tx=8 failed_tx=0 ack_timeouts=0
errors=0 rx_data=8 rx_data_bytes=256 pending=0
```

单元/静态检查：

| 命令 | 结果 |
|---|---|
| `python -m unittest test_rf_comm_client.py -v` in `software/host_client` | PASS，17 tests |
| `python -m unittest -v software.host_client.test_rf_comm_client` from repo root | PASS，17 tests |
| `python software/ps_lwip_bridge/check_ps_bridge_static.py` | `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1` |

全量 simulation gate：

| 文件 | 结果 |
|---|---|
| `reports/simulation_gates_20260626_011929.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| `reports/simulation_gates_20260626_011929.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |

通过项摘录：

- `IR_STREAM_PARALLEL_ASYM_2LANE_PERF_PASS`
- `IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75`
- `ALL_IR_ARRAY_TESTS_PASS`
- `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`
- `[PASS] host_mock_protocol_tests`
- `RF_COMM_SIMULATION_GATES_PASS`

安全状态：

- 本节没有新增实物运行。
- 当前开发板最后一次实物运行仍已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。

结论：

- P2 的 PC/PS 离线 acceptance 能力增强：真实板子不可用时也能验证 PC 端协议、命令流、CSV 证据和 reconnect 流程。
- PC client 的 pending race 已修复，这对真实 TCP/DHCP 链路下的低延迟 ACK/STATUS 响应同样有意义。
- 这仍不等于真实 PS-PC TCP/DHCP 端到端已完成；真实 DHCP/static fallback、真实板端 TCP 5001、PS DMA/PL/IR 端到端仍需硬件条件满足后验证。

## 15. 将 offline mock acceptance 固化进 simulation gate

目的：
- 把第 14 节新增的 `offline_mock` acceptance 从手动验证项提升为自动门禁项。
- 避免后续修改 PC client、acceptance wrapper 或 mock server 时，只跑 unittest 而漏掉完整命令链。
- 进一步降低 P2 PS-PC 链路在真实板端联调前的工具链风险。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `software/host_client/run_acceptance.ps1` | `00426D9D4B6201F64C93ED78C182D571F924ACD6719C714CD7B3894FD57BDAF1` | `offline_mock` 未显式给 `-Port` 时自动选择空闲 localhost TCP 端口 |
| `tools/run_simulation_gates.ps1` | `28BCD83E713C30E3E4295FED51BA1A07747D9EF806C80D2EAD0A5325932EC46E` | 新增 `host_offline_acceptance` 门禁步骤，调用 `run_acceptance.ps1 -Mode offline_mock` |

目标验证：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\software\host_client\run_acceptance.ps1 -Mode offline_mock -Repeat 8 -PayloadSize 32 -ReconnectCycles 2 -TimeoutSeconds 2 -AckTimeoutSeconds 2
```

结果：

- 自动分配端口：`62424`（全量 gate 中的本次运行）。
- `software/host_client/logs/offline_mock_single_lane_20260626_013119.csv` 包含 `ACCEPTANCE_PASS`。
- CSV 摘要：`sent_packets=8`、`acked_tx=8`、`errors=0`、`rx_data=8`、`pending=0`。

全量 simulation gate：

| 文件 | 结果 |
|---|---|
| `reports/simulation_gates_20260626_012712.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| `reports/simulation_gates_20260626_012712.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |

新增门禁证据：

- `reports/simulation_gates_20260626_012712.out.log` 包含 `[PASS] host_offline_acceptance`。
- 同一日志包含 `RF_COMM acceptance mode: offline_mock`、`log_acceptance PASS`、`RF_COMM acceptance mode finished: offline_mock`。
- 同一日志仍包含 `ALL_IR_ARRAY_TESTS_PASS`、`PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`、`[PASS] host_mock_protocol_tests`。

安全状态：

- 本节没有新增实物运行。
- 当前开发板最后一次实物运行仍已烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。

结论：

- P2 的离线 PC/PS acceptance 现在已经是自动 simulation gate 的一部分。
- 当前离线门禁覆盖层级为：RTL/XSim、PS bridge static、host Python compile、host mock unittest、host offline acceptance wrapper。
- 这仍不等于真实 PS-PC TCP/DHCP 端到端完成；硬件可用后还需要用同一 acceptance wrapper 指向真实 DHCP/static IP 地址做板级 smoke、reconnect、soak 和吞吐验证。

## 16. P2 启动链补强、当前单 lane bit/XSA/BOOT 重建和门禁闭合

目的：

- 在不新增实物运行的前提下，把当前 Vivado bitstream、XSA、Vitis ELF 和 BOOT.BIN 重新拉齐。
- 修复项目门禁中 utilization 报告解析过窄的问题，使 Vivado 2023.1 中 `Block RAM Tile` 的小数用量也能被正确解析。
- 清理当前单 lane 顶层下 J11/lane1 空端口约束导致的 CRITICAL WARNING，确保项目门禁不再带着已知约束噪声通过。
- 保留 J11/lane1 的管脚映射文字记录，但当前有效 XDC 只启用真实存在的 lane0 端口；后续生成 2lane 顶层后再重新启用 lane1 约束。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/run_project_gates.ps1` | `CEE2A0F27F0CFF180F46BFB9521343CD8BCE4572D32525CF524EAD9555122AF6` | utilization 解析改为按 Vivado 表格列拆分，支持 BRAM 小数用量 |
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.srcs/constrs_1/new/PORT1.xdc` | `A6137A432F313A26B04F48CE586FAC0A9DEF698DB9285D659C4348047E186F4E` | 当前只启用 J10/lane0；J11/lane1 映射作为注释保留，B_RX1 仍记录为 J11 PIN33 / G15 |

XDC 排错记录：

- `reports/build_current_bitstream_guarded_xdc_20260626_013919.*` 失败原因：helper 使用 `get_ports` 匹配带 `[0]` 的端口名时被 Vivado 作为模式字符处理，lane0 约束没有落上。
- `reports/build_current_bitstream_guarded_xdc_20260626_014212.*` 失败原因：Vivado 将项目 XDC 按约束子集解析，不支持 `proc`、`if` 和自定义 helper 命令。
- 最终方案：有效 XDC 使用 Vivado 支持的纯 `set_property` / `set_false_path` 写法，当前只约束单 lane 顶层真实存在的 `[0]` 端口；lane1/J11 作为注释化映射保留。

构建结果：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| bit/XSA 重建 | `reports/build_current_bitstream_lane0_active_20260626_014516.meta.txt` | `BIT_BUILD_EXIT code=0` |
| BOOT 重建 | `reports/build_boot_image_after_lane0_xdc_20260626_014810.meta.txt` | `BOOT_BUILD_EXIT code=0` |
| 项目门禁 | `reports/project_gates_after_lane0_xdc_20260626_014856.meta.txt` | `PROJECT_GATES_EXIT code=0` |
| 仿真门禁 | `reports/simulation_gates_after_lane0_xdc_20260626_014918.meta.txt` | `SIMULATION_GATES_EXIT code=0` |

当前启动链制品：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `954563915AC0D483DC195D2AAD54119FA5090E87E585A67206EE22B3F34F2F92` | 当前单 lane 有效 bitstream |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.bit` | `954563915AC0D483DC195D2AAD54119FA5090E87E585A67206EE22B3F34F2F92` | bitstream 副本 |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `51C7EC7497F8026346E8575EBDEA57CADEF3BF12684BD4C0DDD20A1C6F1B9F29` | 当前硬件平台 |
| `software/_vitis_ws/rf_comm_ps_bridge/Debug/rf_comm_ps_bridge.elf` | `0BC5F1C1CAF19CA9B6E8E037D95AB202218290C9C29FE82EE05F45ED30E38D79` | 当前 PS bridge ELF |
| `software/_vitis_ws/design_shiboqi_wrapper/export/design_shiboqi_wrapper/sw/design_shiboqi_wrapper/boot/fsbl.elf` | `25009DAB32EB6F7B55ABC25091D1C64CC3D89B3B2F0407AA5DBEAD36FB9C84E9` | FSBL |
| `software/_boot/rf_comm_boot.bif` | `A6E9C4AE4BA31195331A18FD6BB174CAE9E1E9285A5E4F51F8030203C1F51223` | BOOT.BIN 输入描述 |
| `software/_boot/BOOT.BIN` | `6CF2DEF65DB0A40D7EC46F6486DA911E9248925CDCB9AB2AE047371A09687CC7` | 当前 SD 启动镜像 |

项目门禁要点：

- `reports/project_gates_after_lane0_xdc_20260626_014856.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS`。
- constraint hash、bitstream、XSA、PS ELF、timing、utilization、Vivado 日志、host py_compile、host unittest、host offline acceptance、BOOT 新鲜度均 PASS。
- timing：`WNS=1.153`、`WHS=0.027`、`setup_fail=0`、`hold_fail=0`。
- utilization：`LUT=44.04%`、`FF=24.89%`、`BRAM=9.17%`、`DSP=0.00%`。
- `synth_1/runme.log`、`impl_1/runme.log`、`TFDU_VFIR_Client_Array/vivado.log` 均未检出 ERROR/CRITICAL WARNING。

仿真门禁要点：

- `reports/simulation_gates_after_lane0_xdc_20260626_014918.out.log` 包含 `RF_COMM_SIMULATION_GATES_PASS`。
- 同一日志包含 `ALL_IR_ARRAY_TESTS_PASS`。
- 同一日志包含 `IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75`。
- 同一日志包含 `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`。
- 同一日志包含 `[PASS] host_mock_protocol_tests` 和 `[PASS] host_offline_acceptance`。

安全状态：

- 本节没有新增上板实物实验，没有烧写开发板。
- 当前记录中的最后实物状态仍是已经烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 后续如重新进入小板实物运行，仍必须遵守连续运行不超过 5 分钟、实验结束后烧入 shutdown bitstream 的限制。

结论：

- 当前单 lane lane0 的 bit/XSA/BOOT 启动链已经完成一次离线闭合，并通过项目门禁和完整 simulation gate。
- 这次闭合不能宣称 lane1/2-lane 实物已经恢复；当前有效硬件构建只启用 lane0。
- J11/lane1 管脚映射已经保留在 XDC 注释中，等 2lane 顶层重新生成并确认端口存在后，可以再启用并进行短窗口硬件验证。

## 17. P0-5 RX microscope 增强和当前镜像重建

目的：

- 在不新增实物运行的前提下，继续补强 P0-5 RX microscope。
- 让 AXI wrapper 能通过寄存器读出更明确的 RX 分层诊断，不只依赖 ILA 或外部波形判断。
- 增加 session mismatch 场景下的 AXI 级别仿真，证明接收端能把错误归入 header/session 层，而不是误判为物理层完全不通。
- 将新增 RTL/debug 能力重新纳入当前 bit/XSA/BOOT 和项目门禁。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `IPs/ip_ir_array/src/ir_array_top_axi.sv` | `7CE865D4D48B014BFF8A0DB6FD55956C38F028F4B3BD3AF29FF02891A9889B2A` | 接入 RX debug status，并提高 AXI debug 读数对 D/E 类诊断的可见性 |
| `IPs/ip_ir_array/src/ir_array_rx_mgr.sv` | `52D687AEBF4FB1CAC1E147F1548FB84A6FD5461EA85D0DAC52593AA712F20E00` | 新增 `0xD9` 数据接收并排队 ACK 的 debug code |
| `IPs/ip_ir_array/sim/tb_ir_array_top_axi_rx_microscope.sv` | `2E032D04A14458867FC00DAAC478CEE1A618C598E6BAF628A67F5CFE216C514A` | 新增 AXI RX microscope session mismatch 仿真 |
| `tools/decode_p0_debug.py` | `7B2FB972B5B5856919E64B70329471EDF08398B82D973553E150928C8E4CF510` | 增加 `D9` 解码和 ACK/path 判读 |
| `IPs/ip_ir_array/run_loopback_single_lane.ps1` | `E4AE98ED4E945D8522C01245C824202421BAD365F45F66AFB0C3E48E60FF736A` | 新增 `axi_rx_microscope` 测试入口，并纳入 `all` |
| `tools/write_simulation_evidence_report.ps1` | `86FCFE2EB024E1428A972C4FF6407A46D5AE7D591C6D4BEF65033C754E49735F` | 新增 `axi_rx_microscope` 证据项 |

新增仿真证据：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| 单项仿真 | `sim_work/axi_rx_microscope_axi_top/axi_rx_microscope_axi_top.sim/sim_1/behav/xsim/simulate.log` | PASS |
| 全量仿真门禁 | `reports/simulation_gates_rx_microscope_20260626_015858.meta.txt` | `SIMULATION_GATES_EXIT code=0` |
| 全量仿真门禁输出 | `reports/simulation_gates_rx_microscope_20260626_015858.out.log` | `RF_COMM_SIMULATION_GATES_PASS` |

关键 PASS：

```text
AXI_RX_MICROSCOPE_SESSION_MISMATCH_PASS sticky=0x00000010 rx_good=0x00000001 debug=0xd4201203
ALL_IR_ARRAY_TESTS_PASS
PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1
[PASS] host_offline_acceptance
RF_COMM_SIMULATION_GATES_PASS
```

判读：

- `sticky=0x00000010` 表示 session mismatch 被 AXI wrapper 归入 header/session 类状态。
- `rx_good=0x00000001` 表示接收端确实看到过有效接收事件，不是单纯无光、无边沿或物理完全断开。
- `debug=0xd4201203` 给出 RX debug 分类证据，可作为后续真实板级 ILA/AXI 读数的对照。

当前启动链重建结果：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| bit/XSA 重建 | `reports/build_current_bitstream_rx_microscope_20260626_020413.meta.txt` | `BIT_BUILD_EXIT code=0` |
| BOOT 重建 | `reports/build_boot_image_rx_microscope_20260626_020656.meta.txt` | `BOOT_BUILD_EXIT code=0` |
| 项目门禁 | `reports/project_gates_rx_microscope_20260626_020730.meta.txt` | `PROJECT_GATES_EXIT code=0` |

当前制品 SHA256：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `E21197652D31800386D7A72DB1EC8FA6B26E87D43087F64002B9FE5C48EC8986` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.bit` | `E21197652D31800386D7A72DB1EC8FA6B26E87D43087F64002B9FE5C48EC8986` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `F374C50005561257005CE6C02CD8503369A38B82BE92457F037DBDE3DA012FBB` |
| `software/_vitis_ws/rf_comm_ps_bridge/Debug/rf_comm_ps_bridge.elf` | `0BC5F1C1CAF19CA9B6E8E037D95AB202218290C9C29FE82EE05F45ED30E38D79` |
| `software/_boot/BOOT.BIN` | `6CF2DEF65DB0A40D7EC46F6486DA911E9248925CDCB9AB2AE047371A09687CC7` |

项目门禁要点：

- `reports/project_gates_rx_microscope_20260626_020730.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS`。
- timing：`WNS=1.153`、`WHS=0.027`、`setup_fail=0`、`hold_fail=0`。
- utilization：`LUT=44.04%`、`FF=24.89%`、`BRAM=9.17%`、`DSP=0.00%`。
- `synth_1/runme.log`、`impl_1/runme.log`、`TFDU_VFIR_Client_Array/vivado.log` 均未检出 ERROR/CRITICAL WARNING。

安全状态：

- 本节没有新增实物实验，没有烧写开发板。
- 当前记录中的最后实物状态仍是已经烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。

结论：

- P0-5 已从纯离线解码增强，推进到 AXI wrapper 可读的 RX microscope 证据。
- 当前 bit/XSA/BOOT 已包含该 debug 能力，并通过仿真门禁和项目门禁。
- 这仍不能宣称真实 lane1/2-lane 已恢复，也不能宣称真实 PS-PC TCP/DHCP 端到端已完成；后两项仍需后续硬件条件满足后验证。

## 18. 真实 2-lane stream_bidir 顶层恢复、J11 约束启用和离线门禁闭合

目的：

- 推进 P1 前置条件：不再停留在单 lane 顶层，而是恢复一个真实 `[1:0]` TFDU 端口的 2-lane 工程镜像。
- 避开已知超资源的 `IR_PARALLEL_2LANE_MODE=1` 版本，先使用已能在 ZYNQ-7010 落地的 `stream_bidir` 2-lane 结构。
- 启用 J11/lane1 约束，包括 `B_RX1 = J11 PIN33 / G15` 的当前排障映射。
- 在不上板、不驱动小板的前提下完成 bit/XSA/BOOT、项目门禁和仿真门禁闭合。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

2-lane 配置：

```text
IR_LANE_COUNT=2
IR_B_MODE=stream_bidir
IR_PARALLEL_2LANE_MODE=0
IR_CNT_CHIP_MAX=15
IR_CNT_PREAMBLE=64
IR_RX_DETECT_WINDOW=0..10
IR_B_SESSION_ID=0x2203
IR_B_RX_LANE_MASK=3
IR_B_TX_LANE_MASK=3
IR_B_ACK_LANE_MASK=3
IR_B_EXPECTED_A_LANE_MASK=3
IR_B2A_ENABLE=1
IR_B2A_FREE_RUN=0
IR_B_BACKOFF_SLOT_CYCLES=100000
IR_GUARD_CYCLES=1408
```

修改/生成文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.srcs/constrs_1/new/PORT1.xdc` | `13B233937908A2DF171AE6DD7D97DFC1FA342C01A76434DCB4EC4EDF1BFAD98C` | 启用 J11/lane1 A/B 约束，B_RX1 使用 J11 PIN33 / G15 |
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.srcs/sources_1/imports/hdl/design_shiboqi_wrapper.v` | `D12F8C09EA6E381451DBE3D3817191C3496035F1783058B0BCEA56CA424397D0` | wrapper 端口已变为 `[1:0]` |

配置与构建结果：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| 2-lane BD/Wrapper 配置 | `reports/vivado_config_2lane_stream_bidir_rxmicroscope_20260626_021234.meta.txt` | `CONFIG_2LANE_EXIT code=0` |
| 2-lane bit/XSA 构建 | `reports/build_current_bitstream_2lane_stream_bidir_rxmicroscope_20260626_021323.meta.txt` | `BIT_BUILD_2LANE_EXIT code=0` |
| 2-lane BOOT 重建 | `reports/build_boot_image_2lane_stream_bidir_rxmicroscope_20260626_021717.meta.txt` | `BOOT_BUILD_2LANE_EXIT code=0` |
| 项目门禁 | `reports/project_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022203.meta.txt` | `PROJECT_GATES_2LANE_SEQ_EXIT code=0` |
| 仿真门禁 | `reports/simulation_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022622.meta.txt` | `SIMULATION_GATES_2LANE_SEQ_EXIT code=0` |

当前 2-lane 制品 SHA256：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `E4787A9E73E855ED9D7582B4AE1B34CFE77835E418AC4DA2316E4CD33DF17382` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.bit` | `E4787A9E73E855ED9D7582B4AE1B34CFE77835E418AC4DA2316E4CD33DF17382` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `EBF2C2D2CC985FA31B4858E0FB7CF20E6DFDAFDDC89911A4B0559659AD02D7B6` |
| `software/_boot/BOOT.BIN` | `C6F4A05A0DEC7D6270D9BF2BD5E8E75533D9B2EEC633A8F3B65F23FF4746EF23` |

项目门禁要点：

- `reports/project_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022203.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS`。
- constraint hash：桌面和仓库内约束目标文件均 PASS。
- bitstream：`2083858 bytes, 2026-06-26 02:16:50`。
- XSA：`696805 bytes, 2026-06-26 02:17:08`。
- BOOT.BIN：`2319112 bytes, 2026-06-26 02:17:18`。
- timing：`WNS=1.218`、`WHS=0.018`、`setup_fail=0`、`hold_fail=0`。
- utilization：`LUT=61.27%`、`FF=32.83%`、`BRAM=9.17%`、`DSP=0.00%`。
- `[PASS] host_offline_acceptance` 仍通过。

仿真门禁要点：

- `reports/simulation_gates_2lane_stream_bidir_rxmicroscope_seq_20260626_022622.out.log` 包含 `RF_COMM_SIMULATION_GATES_PASS`。
- 同一日志包含 `ALL_IR_ARRAY_TESTS_PASS`。
- 同一日志包含 `AXI_RX_MICROSCOPE_SESSION_MISMATCH_PASS sticky=0x00000010 rx_good=0x00000001 debug=0xd4201203`。
- 同一日志包含 `IR_STREAM_PARALLEL_ASYM_2LANE_PERF_PASS`。
- 同一日志包含 `IR_STREAM_AXI_BIDIR_SINGLE_LANE_PASS a_to_b=90 b_to_a=75`。
- 同一日志包含 `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1`。
- 同一日志包含 `[PASS] host_offline_acceptance`。

排错备注：

- 曾并行启动 project gate 和 simulation gate，XSim 在同一个 `xsim.dir` 下抢占输出文件，触发一次 Windows 文件锁 `Permission denied`。
- 随后清查无残留 Vivado/XSim 进程，并顺序重跑 project gate 与 simulation gate，二者均 PASS。
- 因此最终结论以顺序门禁 `*_seq_20260626_022203` 和 `*_seq_20260626_022622` 为准。

安全状态：

- 本节没有新增实物实验，没有烧写开发板。
- 当前记录中的最后实物状态仍是已经烧入 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。
- 当前仓库内主 bitstream 已变为 2-lane stream_bidir 镜像；后续若上板测试小板，仍必须遵守 5 分钟限制，并在测试后立即烧入 shutdown bitstream。

结论：

- P1 的一个关键前置条件已经完成：当前工程不再只是单 lane 顶层，已经恢复为真实 2-lane 端口，并且 J11/lane1 约束已启用。
- 2-lane stream_bidir 版本在 ZYNQ-7010 上通过实现、timing、资源、BOOT 和离线门禁。
- 这仍不能宣称 2-lane 实物物理路径已经恢复；真实 lane0/lane1 A-B、B-A 通断矩阵还没有在本轮上板验证。
- `IR_PARALLEL_2LANE_MODE=1` 的 parallel 版本仍是超资源路径，不作为当前可上板镜像。

## 19. 2-lane ILA 可观测镜像和第一次硬件窗口尝试

目的：

- 在第 18 节 2-lane stream_bidir 镜像基础上，补加被动 `ila_2lane_phy`，用于后续 lane0/lane1 A-B、B-A 通断矩阵。
- 尝试执行一次最短窗口 `a_tx_lane0` 硬件捕获，并验证安全脚本在失败情况下的 shutdown 行为。
- 如果硬件/JTAG 不可用，停止继续烧写测试 bit，避免小板被无意义长时间驱动。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/run_2lane_hw_prearmed_ila_safe.ps1` | `AE3655F057E743954361285B1733A9F735691768571E9FC4466AD3D1CAC6BCE6` | 增强失败收尾：init/ILA 抛错后仍写出 shutdown 尝试和最终错误摘要 |

语法检查：

```text
RUN_2LANE_SAFE_PS_PARSE_OK
```

2-lane ILA 镜像构建：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| 添加 2-lane ILA | `reports/vivado_add_2lane_phy_ila_current_20260626_023259.meta.txt` | `ADD_2LANE_ILA_EXIT code=0` |
| bit/XSA 重建 | `reports/build_current_bitstream_2lane_stream_bidir_ila_20260626_023331.meta.txt` | `BIT_BUILD_2LANE_ILA_EXIT code=0` |
| BOOT 重建 | `reports/build_boot_image_2lane_stream_bidir_ila_20260626_023734.meta.txt` | `BOOT_BUILD_2LANE_ILA_EXIT code=0` |
| 项目门禁 | `reports/project_gates_2lane_stream_bidir_ila_20260626_024119.meta.txt` | `PROJECT_GATES_2LANE_ILA_EXIT code=0` |

当前带 ILA 制品 SHA256：

| 文件 | SHA256 |
|---|---|
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.bit` | `96963E740D9B115C0E60A89B355C9EB775716F2DE06E30C0EB6048DF441DAA5B` |
| `TFDU_VFIR_Client_Array/TFDU_VFIR_Client.runs/impl_1/design_shiboqi_wrapper.ltx` | `32805D7AE4FDFB411F74E821A6CCF99702C879E825318548224640062F18913C` |
| `TFDU_VFIR_Client_Array/design_shiboqi_wrapper.xsa` | `E7A137FA96507C1E1A3290B1A548711E4F560E68834FF490588946B8FFA3D17F` |
| `software/_boot/BOOT.BIN` | `4C753690E35F5D3ED2F611E3D83602BF4A94CE260E34FBF241F24378BBF7C30D` |

项目门禁要点：

- `reports/project_gates_2lane_stream_bidir_ila_20260626_024119.out.log` 包含 `RF_COMM_PROJECT_GATES_PASS`。
- timing：`WNS=1.635`、`WHS=0.01`、`setup_fail=0`、`hold_fail=0`。
- utilization：`LUT=69.90%`、`FF=39.61%`、`BRAM=45.83%`、`DSP=0.00%`。
- bitstream：`2083858 bytes, 2026-06-26 02:37:03`。
- XSA：`751722 bytes, 2026-06-26 02:37:22`。
- BOOT.BIN：`2319112 bytes, 2026-06-26 02:37:34`。
- `[PASS] host_offline_acceptance` 仍通过。

硬件窗口尝试：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| `a_tx_lane0` 安全脚本 | `reports/2lane_prearmed_a_tx_lane0_20260626_023755.summary.txt` | 未进入测试；init 阶段失败 |
| init 错误 | `reports/xsct_init_2lane_prearmed_a_tx_lane0_20260626_023755.err.log` | `Cannot reset APU. Timeout waiting DAP to reconnect after reset` |
| shutdown retry | `reports/program_tfdu_shutdown_retry_after_failed_init_20260626_023946.log` | `HW_TARGET_COUNT 0`，未检测到 hw target |
| UART | `reports/uart_2lane_prearmed_a_tx_lane0_20260626_023755.log` | 无板端输出 |

关键判读：

- init 日志停在 `Resetting PS system`，没有出现 `Programming FPGA`。
- 因此本次没有证据表明 2-lane 测试 bitstream 被烧入 FPGA，也没有形成有效 ILA capture。
- shutdown 在 finally 中被调用，但 Vivado 硬件管理器没有枚举到 Zynq/FPGA target，后续单独 retry 仍为 `HW_TARGET_COUNT 0`。
- Windows PnP 能看到 `USB Serial Converter` 和 `CP210x USB to UART Bridge (COM3)`，但 hw_server 没有看到可编程器件；这更像开发板电源/JTAG 链路/启动状态问题，而不是红外协议失败。

安全状态：

- 本轮硬件脚本在 `rst -system` 阶段失败，未到 `Programming FPGA`。
- 由于没有检测到 hw target，shutdown retry 无法重新烧入 shutdown bitstream。
- 但当前没有证据显示测试 bit 被烧入或小板被当前 2-lane bit 驱动；最后一次成功记录的实物关闭状态仍是此前的 `shutdown_bitstream/tfdu_shutdown_j10_j11.bit`。

结论：

- 带 ILA 的 2-lane 可观测镜像已经完成，并通过项目门禁，后续可直接用于硬件矩阵。
- 当前硬件验证未开始，原因是 JTAG/板卡未被 Vivado 枚举到可编程 Zynq/FPGA 目标。
- 在恢复 JTAG target 之前，不应继续重复硬件测试；应先确认开发板上电、JTAG 线、启动模式/复位状态和 Vivado Hardware Manager 能看到 `xc7z010`。

## 20. 无烧写硬件预检和 2-lane 安全脚本前置挡板

目的：

- 在 JTAG/板卡枚举不稳定时，先做无烧写预检，只有 Vivado Hardware Manager 能看到 `xc7z010` 后才允许进入 2-lane 硬件测试。
- 避免在 `HW_TARGET_COUNT 0` 的状态下重复启动 XSCT reset/program 流程。
- 将“当前硬件不可测试”的原因固化成可重复证据，而不是只依赖人工观察。

强约束状态：

- 本节未修改 `项目约束(目标）.txt`。
- 约束文件 SHA256 仍为 `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11`。

新增/修改文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/check_hw_target.tcl` | `A979EFD9F9E52D5776481DF2AEC121C090993D02C0E4B8EA99AEACAF255E29AF` | Vivado 无烧写硬件枚举预检，只检查 hw target / device |
| `tools/check_hw_target.ps1` | `7A39216C084B895F09ED85F4D3CAECF17A290AE77A9DBC9FB18E3D0C4DC5A1F5` | 包装 Tcl 预检，同时记录 Windows PnP、COM 口和 Vivado 输出 |
| `tools/run_2lane_hw_prearmed_ila_safe.ps1` | `37AAD2B626625CA49E6AF117DCDE40C0E6822C51AD3D86300B605A4020190D29` | 默认先跑预检；预检失败时直接退出，不烧写、不启动 UART/ILA/ELF |

语法检查：

```text
PARSE_OK .\tools\run_2lane_hw_prearmed_ila_safe.ps1
PARSE_OK .\tools\check_hw_target.ps1
```

无烧写预检结果：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| 预检 wrapper | `reports/hw_target_preflight_20260626_024825.summary.txt` | `VIVADO_PREFLIGHT_EXIT=2` |
| Vivado target | 同上 | `HW_PREFLIGHT_TARGET_COUNT 0`、`HW_PREFLIGHT_RESULT FAIL_NO_TARGET` |
| 串口 | 同上 | `SERIAL_PORTS=COM1,COM3`、`COM_PORT_PRESENT=1` |
| USB/JTAG | 同上 | PnP 可见 `USB Serial Converter`，VID `0403` |
| CP210x | 同上 | PnP 可见 `Silicon Labs CP210x USB to UART Bridge (COM3)` |

2-lane 安全脚本前置挡板验证：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| `a_tx_lane0` 预检挡板 | `reports/2lane_prearmed_a_tx_lane0_20260626_024843.summary.txt` | `PREFLIGHT_EXIT=2` |
| 是否进入烧写 | 同上 | `PREFLIGHT_BLOCKED_NO_PROGRAMMING=1` |
| Vivado target | 同上 | `HW_PREFLIGHT_TARGET_COUNT 0`、`HW_PREFLIGHT_RESULT FAIL_NO_TARGET` |

判读：

- 当前 Windows USB 层能看到 Digilent JTAG USB 和 COM3，但 Vivado Hardware Manager 仍看不到可操作 hw target。
- 新的安全脚本会在这种状态下直接挡住，不会进入 `program_fpga_init_ps7.tcl`，也不会启动 UART 捕获、ILA 捕获或 ELF。
- 本节没有烧写 FPGA，没有驱动 TFDU 小板，也不需要 shutdown bitstream。

进程收尾：

- 本次预检后曾残留 `hw_server/cs_server` 进程。
- 已清理相关 Xilinx 后台进程；最终未发现 `hw_server`、`cs_server`、`vivado`、`rdi_xsct`、`xsim`、`xelab`、`xvlog` 残留。

结论：

- P1 硬件线的下一步判据被固化为：先运行 `tools/check_hw_target.ps1`，必须看到 `HW_PREFLIGHT_RESULT PASS` 和 `HW_PREFLIGHT_ZYNQ ...`，才继续 2-lane 通断矩阵。
- 在当前 `FAIL_NO_TARGET` 状态下，不继续重复硬件测试。

## 21. 2026-06-26 02:55 JTAG 目标复测与驱动状态检查

目的：

- 响应本轮“再试一下”的硬件复测请求。
- 仅做无烧写预检，不启动 2-lane bitstream、不启动 UART/ILA/ELF，不驱动 TFDU 小板。

复测结果：

| 项 | 文件或日志 | 结果 |
|---|---|---|
| 1 MHz 预检 | `reports/hw_target_preflight_20260626_025118.out.log` | `HW_PREFLIGHT_TARGET_COUNT 0`、`HW_PREFLIGHT_RESULT FAIL_NO_TARGET` |
| 500 kHz 预检 | `reports/hw_target_preflight_20260626_025505.out.log` | `HW_PREFLIGHT_TARGET_COUNT 0`、`HW_PREFLIGHT_RESULT FAIL_NO_TARGET` |
| COM3 | 两次 summary 输出 | `COM_PORT_PRESENT=1` |
| USB/JTAG 枚举 | Windows PnP | `USB Serial Converter` 可见，VID `0403`、PID `6014` |
| CP210x 串口 | Windows PnP | `Silicon Labs CP210x USB to UART Bridge (COM3)` 可见 |
| 设备重扫 | `pnputil /scan-devices` | 需要管理员权限，返回 `Access is denied` |

驱动状态：

- 系统中存在 Digilent、FTDI、Xilinx cable driver 条目。
- 当前 `USB\VID_0403&PID_6014\210512180081` 实际绑定到 FTDI `oem27.inf`，设备名为 `USB Serial Converter`。
- 尝试运行 Vivado 自带 `install_digilent.exe /S` 未在 120 秒内返回，未生成有效安装日志；未发现残留安装进程。

安全状态：

- 本节没有烧写 FPGA。
- 本节没有启动 TFDU 通信测试。
- 因为没有进入烧写流程，也没有驱动小板，本节不需要烧入 shutdown bitstream。

判读：

- 当前阻塞点仍是 Vivado Hardware Manager 看不到 JTAG hw target，而不是 2-lane 红外物理通断结论。
- 在 `HW_PREFLIGHT_RESULT PASS` 之前，不继续执行 2-lane AB/BA 实物矩阵。
- 需要优先检查开发板供电、JTAG 线、JTAG/USB 口是否接对、BOOT/JTAG 相关拨码或跳线、PS/PL 复位状态，以及是否需要用管理员权限重新安装或重绑定 Digilent/JTAG 驱动。

## 22. 2026-06-26 02:58 JTAG/USB 诊断脚本固化

目的：

- 将当前 JTAG target 不可见问题固化成可重复诊断工具。
- 区分 Windows USB/PnP 可见、驱动包存在、设备驱动绑定、Vivado hw_server target 可见这几个层级。
- 在恢复 JTAG 前继续保持不烧写、不驱动 TFDU 的安全策略。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/diagnose_jtag_usb.ps1` | `12854A79B5FE2DEF7B65A501CF91DAC577BD36F7981B4320B15F18F7915E288D` | JTAG/USB/驱动/Vivado preflight 综合诊断脚本 |
| `JTAG目标枚举诊断_20260626.md` | `243DEA26E08DD66F90096962D48AB75975BB8E12772D63945C76B839F1FF34B9` | 本轮中文诊断报告 |

诊断证据：

| 项 | 文件 | 结果 |
|---|---|---|
| 完整诊断 | `reports/jtag_usb_diag_20260626_025747.summary.txt` | 脚本完成；1 MHz 和 500 kHz Vivado preflight 均 `FAIL_NO_TARGET` |
| 快速自检 | `reports/jtag_usb_diag_20260626_025828.summary.txt` | `-SkipVivadoPreflight` 成功，频率参数解析修正通过 |
| 脚本语法检查 | PowerShell parser | `PARSE_OK .\tools\diagnose_jtag_usb.ps1` |

关键判读：

- `COM_PORT_PRESENT=1`，COM3 正常可见。
- `VID_0403&PID_6014` 设备可见，但当前绑定为 FTDI `oem27.inf` / `FTDIBUS`，设备名 `USB Serial Converter`。
- 系统中存在 Digilent 驱动包和 Xilinx cable driver 包，但 connected devices 中没有 Digilent/Xilinx target 字样。
- Vivado 仍返回 `HW_PREFLIGHT_TARGET_COUNT 0`，因此本节没有进入任何烧写或 TFDU 驱动流程。

结论：

- 当前 plan 的硬件线仍停在 JTAG target 枚举门槛。
- 新增诊断脚本可以作为每次人工调整线缆/拨码/驱动后的第一步复测入口。
- 只有 `tools/check_hw_target.ps1` 或 `tools/diagnose_jtag_usb.ps1` 中出现 `HW_PREFLIGHT_RESULT PASS` 和 `HW_PREFLIGHT_ZYNQ ...` 后，才恢复 2-lane AB/BA 实物矩阵测试。

## 23. 2026-06-26 03:03 2-lane ILA 矩阵离线分析工具

目的：

- 补齐 plan 中 P1-1 lane mapping matrix 的自动判读工具。
- 在 JTAG 恢复后，新的 ILA CSV 可以立即判定 A->B lane0/lane1、B->A lane0/lane1 是否有对应原始脉冲。
- 当前只分析既有 CSV 文件，不烧写 FPGA，不启动 TFDU 小板。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/analyze_2lane_ila_csv.py` | `BAD8CB3C9444D7407D60D865ED4478090EB60C59367248DADA09812003F9130E` | 2-lane ILA CSV 原始脉冲、交叉活动、延迟、期望方向判读工具 |

验证命令：

```text
python -m py_compile .\tools\analyze_2lane_ila_csv.py
python .\tools\analyze_2lane_ila_csv.py .\reports\ila_2lane_prearmed_b_tx_nonzero_20260606_103701.csv .\reports\ila_2lane_prearmed_a_tx_lane0_20260625_172233.csv .\reports\ila_2lane_prearmed_a_tx_lane1_20260625_225024.csv --out .\reports\ila_2lane_matrix_analysis_sample_20260626.md
python .\tools\analyze_2lane_ila_csv.py --limit 20 --out .\reports\ila_2lane_matrix_analysis_latest20_20260626.md
```

验证结果：

| 项 | 文件 | SHA256/结果 |
|---|---|---|
| Python 编译 | `tools/analyze_2lane_ila_csv.py` | `PY_COMPILE_OK` |
| 样例矩阵报告 | `reports/ila_2lane_matrix_analysis_sample_20260626.md` | `CCD49FD8FB08BCEB7A96464BD9522D1A656369E65D4D56E7E0D3D3421EEFF093` |
| 最近 20 个 ILA 总览 | `reports/ila_2lane_matrix_analysis_latest20_20260626.md` | `3D1746A1DAC3B2E39E821AE80528C16A25EA18447E9BB911B3F321B4744CE405` |

样例判读：

| CSV | 自动判读 |
|---|---|
| `ila_2lane_prearmed_b_tx_nonzero_20260606_103701.csv` | `PASS_ANY_B_TO_A`，识别到 `B_TO_A_LANE0` 原始脉冲路径 |
| `ila_2lane_prearmed_a_tx_lane0_20260625_172233.csv` | `PASS_EXPECTED_RAW`，A->B lane0 有对应原始脉冲 |
| `ila_2lane_prearmed_a_tx_lane1_20260625_225024.csv` | `FAIL_EXPECTED_RAW`，A TX lane1 有脉冲但 B_RX1 无对应活动 |

最近 20 个 ILA 总览的直接含义：

- 历史和部分 2026-06-25 capture 证明 A->B lane0、A->B lane1、B->A 某些路径都曾出现原始脉冲。
- 近期 lane1 A->B 多次出现 `FAIL_NO_RX_ACTIVITY`，因此 lane1 仍必须在 JTAG 恢复后重新做物理矩阵。
- 该工具只能证明 raw pulse 是否存在，不能替代协议层 `rx_ok`、CRC、session/mask、ACK gate 判断。

结论：

- P1-1 的自动化判读工具已准备好。
- P1-1 的实物矩阵本身仍未完成，因为当前 JTAG target 不可见。

## 24. 2026-06-26 03:08 2-lane 矩阵安全总控脚本

目的：

- 将 P1-1 lane mapping matrix 的执行入口固化为一个安全总控脚本。
- 总控脚本先做 JTAG/Vivado preflight；只有解析到 `HW_PREFLIGHT_RESULT PASS` 和 `HW_PREFLIGHT_ZYNQ` 才进入单次硬件 run。
- 每个单次 run 仍复用 `tools/run_2lane_hw_prearmed_ila_safe.ps1`，由该脚本负责每次运行后的 shutdown。
- 矩阵 run 结束后自动调用 `tools/analyze_2lane_ila_csv.py` 生成 ILA 矩阵报告。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/run_2lane_matrix_safe.ps1` | `FA2779BEA5823F2CE1403A64D81745DCD11C15F70893F2AAC83A9DB4EDD5CFC0` | 2-lane AB/BA 矩阵安全总控脚本 |

目标执行入口：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_2lane_matrix_safe.ps1 -ComPort COM3 -TriggerModes a_tx_lane0,a_tx_lane1,b_tx_nonzero -StopOnFail
```

验证过程：

| 项 | 文件 | 结果 |
|---|---|---|
| 语法检查 | PowerShell parser | `PARSE_OK .\tools\run_2lane_matrix_safe.ps1` |
| 初版挡板验证 | `reports/2lane_matrix_safe_20260626_030520.summary.txt` | 暴露子 PowerShell 进程退出码不可作为 Vivado 预检通过依据；单次 run 均被 `PREFLIGHT_BLOCKED_NO_PROGRAMMING=1` 挡住，未烧写 |
| 修正后挡板验证 | `reports/2lane_matrix_safe_20260626_030720.summary.txt` | 总控层直接解析到 `HW_PREFLIGHT_RESULT FAIL_NO_TARGET` 并停止 |

最终验证关键输出：

```text
MATRIX_PREFLIGHT_PROCESS_EXIT=0
MATRIX_PREFLIGHT_MATCH=VIVADO_PREFLIGHT_EXIT=2
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
MATRIX_PREFLIGHT_PASS_PARSED=0
MATRIX_PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
```

安全状态：

- 修正后的最终验证没有进入任何单次硬件 run。
- 没有生成新的 ILA CSV。
- 没有烧写 FPGA。
- 没有驱动 TFDU 小板。
- 预检后残留的 `hw_server/cs_server` 已清理。

结论：

- P1-1 的实物矩阵总控入口已经准备好。
- 当前仍不能执行真正矩阵，因为 Vivado 还不能枚举 `xc7z010`。
- JTAG 恢复后，该脚本将按 `a_tx_lane0`、`a_tx_lane1`、`b_tx_nonzero` 顺序执行，并自动生成 ILA 矩阵分析报告。

## 25. 2026-06-26 03:10 PS/PC 离线通信门禁

目的：

- 推进 plan 中 PS 与电脑上位机之间 TCP/DHCP/重连/协议一致性的离线验证。
- 当前只验证源码静态约束、host client 单元测试和本地 mock TCP server acceptance。
- 不连接开发板以太网，不烧写 FPGA，不驱动 TFDU 小板。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/run_ps_pc_offline_gates.ps1` | `7FA1DC27D476ED8DE99E4B9C577E165BDC38FF588A40C5B4E61909B64D3A5765` | PS/PC 离线门禁总控脚本 |

门禁命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_offline_gates.ps1 -Repeat 64 -PayloadSize 96 -ReconnectCycles 4 -TimeoutSeconds 5.0
```

门禁结果：

| 项 | 证据 | 结果 |
|---|---|---|
| PS bridge 静态检查 | `reports/ps_pc_offline_gates_20260626_031017.static.log` | `PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1` |
| host client 单元测试 | `reports/ps_pc_offline_gates_20260626_031017.unittest.log` | `Ran 17 tests ... OK` |
| offline mock acceptance | `reports/ps_pc_offline_gates_20260626_031017.acceptance.log` | `acceptance PASS`、`log_acceptance PASS` |
| 统一 summary | `reports/ps_pc_offline_gates_20260626_031017.summary.txt` | `PS_PC_OFFLINE_GATES_PASS static=1 unittest=1 offline_mock=1` |

关键 acceptance 数据：

```text
sent_packets=64
sent_bytes=6144
tx_packets=64
rx_data=64
rx_data_bytes=6144
errors=0
ack_timeouts=0
reconnect_cycles=4
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/ps_pc_offline_gates_20260626_031017.summary.txt` | `0AE290AC6099BDA6E756B1915527C300FB5BF9B63FE86B60FAFA4C2B31FEF33A` |
| `reports/ps_pc_offline_acceptance_20260626_031017/offline_mock_single_lane_20260626_031021.csv` | `90FAC55ADC390224327762D3884C79D9D53B0809855DDB0627B1E9D4C12FF29B` |

判读：

- 可以证明 host 协议封包、ACK/ERROR/STATUS/RX_DATA 解析、CSV acceptance 分析、重连流程和 PS bridge 源码中的 TCP/DHCP/静态 fallback/reconnect 关键逻辑没有离线回归。
- 不能证明真实 AX7010 PS 以太网、真实 DHCP 获取、真实 TCP 连接、真实 PL/IR 数据链路已经打通。
- 真实 PS/PC smoke 仍等待 JTAG/开发板链路恢复后执行。

## 26. 2026-06-26 03:14 当前 plan 完成度自动审计

目的：

- 将“完成 plan”拆成可重复验证的证据表，避免把离线 PASS、构建 PASS、raw pulse PASS 和真实硬件闭环 PASS 混淆。
- 生成当前状态的 Markdown/JSON 审计报告。
- 当前只读取已有文件和日志，不烧写 FPGA，不启动 TFDU 小板。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/audit_plan_completion.py` | `30A50A5463A01119A3AD0E6F5F8C08073BD05E5D2A8C86F9A4B1A9A92DAB424A` | 当前 plan 完成度自动审计脚本 |

执行命令：

```powershell
python .\tools\audit_plan_completion.py --out .\reports\plan_completion_audit_current_20260626.md --json .\reports\plan_completion_audit_current_20260626.json
```

生成结果：

| 文件 | SHA256 |
|---|---|
| `reports/plan_completion_audit_current_20260626.md` | `593EA8025F68321A305951F614FC0C5929EA58C79042C8FDB4CADC886296B307` |
| `reports/plan_completion_audit_current_20260626.json` | `89C4737ED79C48EA8682D1A01293DB96B1A4CE89DC5C76F9B53AF05F8EC32370` |

当前统计：

```text
PASS=7
WAITING_HARDWARE=5
NOT_STARTED=3
MISSING_OR_UNKNOWN=0
TOTAL=15
```

当前可宣称完成/通过：

- 硬约束文件未修改，哈希保持一致。
- `evidence_lock_20260625.csv` 和 `config_diff_known_good_vs_current.md` 已存在。
- 当前 2-lane stream_bidir/rx-microscope 仿真门禁通过。
- 当前 2-lane ILA 镜像 Vivado 项目门禁、时序和资源检查通过。
- 当前 bit/LTX/XSA/BOOT 和 shutdown bitstream 均存在。
- P1-1 安全矩阵 runner 与 ILA CSV 离线分析工具已准备好。
- PS/PC 软件协议离线门禁通过。

当前不能宣称完成：

- Vivado Hardware Manager 能枚举 `xc7z010`。
- 新的 lane0/lane1 A->B 与 B->A 实物矩阵。
- 真实 AX7010 PS-to-PC TCP/DHCP smoke。
- PC -> PS -> PL/IR -> peer -> PS -> PC 端到端链路。
- 当前 2-lane 协议实物恢复。
- stationary soak、旋转 600 rpm/2 小时、4/8-lane 速率扩展。

判读：

- 当前最先需要外部状态改变的是 JTAG/开发板目标枚举恢复。
- 恢复后第一条命令仍是 `tools/run_2lane_matrix_safe.ps1`，而不是直接做长时间或高功率测试。

## 27. 2026-06-26 03:15 JTAG 重试与 2-lane 安全矩阵拦截

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只执行 JTAG/Hardware Manager 目标枚举和安全矩阵入口预检；没有生成或烧写 bitstream，没有驱动 TFDU 小板，因此不属于实物通信实验，也不需要补烧 shutdown bitstream。

执行命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check_hw_target.ps1 -ComPort COM3 -JtagFrequencyHz 1000000
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_2lane_matrix_safe.ps1 -ComPort COM3 -TriggerModes a_tx_lane0,a_tx_lane1,b_tx_nonzero -StopOnFail
```

JTAG 预检结果：

```text
SERIAL_PORTS=COM1,COM3
COM_PORT_PRESENT=1
PNP_DEVICE status=OK class=USB name=USB Serial Converter id=USB\VID_0403&PID_6014\210512180081
PNP_DEVICE status=OK class=Ports name=Silicon Labs CP210x USB to UART Bridge (COM3)
VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
```

2-lane 安全矩阵入口结果：

```text
MATRIX_PREFLIGHT_PROCESS_EXIT=0
MATRIX_PREFLIGHT_MATCH=VIVADO_PREFLIGHT_EXIT=2
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
MATRIX_PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
MATRIX_PREFLIGHT_PASS_PARSED=0
MATRIX_PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/hw_target_preflight_20260626_031534.out.log` | `23910FB616CC747779AC3D175E165A5AD52DA0B5BFCC14F49F6C0DA6B7E80A09` |
| `reports/hw_target_preflight_20260626_031534.err.log` | `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855` |
| `reports/2lane_matrix_safe_20260626_031555.summary.txt` | `461EA1495B586E6047EF9663B7AB4F6EB0A2791D43E0EC44DE384498EE655CA4` |
| `reports/2lane_matrix_safe_20260626_031555.preflight.log` | `5629240648E803A49EE95FB93BB985D2043E69E50711EDA4D273683CC097DB36` |

判读：

- 当前 COM3 串口存在，CP210x UART 可见。
- 当前 FTDI 类 USB 设备可见，但 Vivado Hardware Manager 仍没有枚举到 `xc7z010` JTAG target。
- 安全矩阵脚本按预期在预检失败时停止，未进入任何烧写、触发、采集或 TFDU 驱动步骤。
- 下一步仍然是恢复 Vivado 能枚举到 Zynq JTAG target；在 `HW_PREFLIGHT_RESULT PASS` 和 `HW_PREFLIGHT_ZYNQ ...` 出现前，不应运行真实 2-lane 物理通断/回环实验。

## 28. 2026-06-26 03:18 JTAG 诊断工具增量修复

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只修复诊断脚本与补充无烧写 USB/驱动诊断，没有修改 RTL、XDC、bitstream 或 PS/host 协议代码。

修改内容：

- `tools/diagnose_jtag_usb.ps1` 增加 `SkipVivadoSelfCheck` 作为 `SkipVivadoPreflight` 的别名。
- 修复单个 FTDI6014 设备查询结果被当作标量时，末尾诊断说明不稳定的问题。
- 在诊断摘要中补充非管理员权限提示和 FTDI6014 当前绑定提示。

验证命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\diagnose_jtag_usb.ps1 -SkipVivadoSelfCheck
```

验证结果：

```text
VIVADO_PREFLIGHT_SKIPPED=1
IS_ADMIN=0
FTDI6014_PRESENT=1
FTDI6014_DRIVER=Driver Name: oem27.inf
FTDI6014_DRIVER=Provider Name: FTDI
FTDI6014_DRIVER=Driver Version: 10/28/2024 2.12.36.20
ADMIN_NOTE=Current shell is not elevated; driver re-scan or re-bind operations may require an elevated PowerShell.
FTDI6014_NOTE=Connected VID_0403&PID_6014 is currently an FTDI USB device; Vivado still must enumerate a hw target before any programming.
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `tools/diagnose_jtag_usb.ps1` | `C66609A943B0E280B4BB3A9EAA3AE9987A8668E6179F21CE44E0AD7E2C4E58DB` |
| `reports/jtag_usb_diag_20260626_031859.summary.txt` | `FE339B825F395D48E71B1734D03DD581F84B4A8880BAA34A9E92C6A922B563E5` |

判读：

- 当前 USB/驱动诊断工具已能快速运行且不反复启动 Vivado。
- 当前阻塞点仍是 Vivado Hardware Manager 未枚举到 JTAG target；这不是红外 lane 通断结论。
- 如果需要执行驱动重扫或重绑定，当前普通权限 PowerShell 不足，通常需要管理员权限环境。

## 29. 2026-06-26 03:20 增加 plan 硬件恢复总入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增脚本只编排既有安全步骤：先做 JTAG 门禁；失败则补跑 USB 诊断并刷新完成度审计；成功才调用 2-lane 安全矩阵。当前实测路径停在 JTAG 预检失败处，没有烧写 FPGA，没有驱动 TFDU 小板。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/run_plan_hardware_resume_safe.ps1` | `76A3E4583BED081C2F29564B6767FAE23AEF64FBB5EDC8016F47348EB1F5BFAE` | JTAG 恢复后继续 plan 的安全总入口 |

验证命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_plan_hardware_resume_safe.ps1 -ComPort COM3 -TriggerModes a_tx_lane0,a_tx_lane1,b_tx_nonzero -StopOnFail
```

当前验证结果：

```text
PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
PREFLIGHT_PASS_PARSED=0
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
USB_DIAG_EXIT=0
USB_DIAG_MATCH=VIVADO_PREFLIGHT_SKIPPED=1
USB_DIAG_MATCH=ADMIN_NOTE=Current shell is not elevated; driver re-scan or re-bind operations may require an elevated PowerShell.
USB_DIAG_MATCH=FTDI6014_NOTE=Connected VID_0403&PID_6014 is currently an FTDI USB device; Vivado still must enumerate a hw target before any programming.
AUDIT_MATCH=PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/plan_hw_resume_safe_20260626_032033.summary.txt` | `5526D418026606A1F536344940C1FB4095F3CFF2D3368841E860FACDBF3D7D47` |
| `reports/plan_completion_audit_current_20260626.md` | `0CE39C8E349EC5513E818100DD1885D88816EFB6569BDD17C2E0D06E70D3295F` |
| `reports/plan_completion_audit_current_20260626.json` | `554EA7C4765BD170D4D54C30B7BB9C528DFB8F48E221E0522869105BE9F13C95` |

判读：

- 以后硬件调整完成后，可以优先运行 `tools/run_plan_hardware_resume_safe.ps1`，让脚本自动执行 JTAG 门禁、2-lane 安全矩阵和完成度审计。
- 当前这次运行未进入 `MATRIX_START`，因此没有进行 2-lane 实物通信实验。
- 当前 plan 未完成，继续等待 JTAG target 可枚举后恢复实物验证。

## 30. 2026-06-26 03:24 增加 JTAG 驱动修复 dry-run 助手

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增工具只面向 JTAG/USB 驱动恢复，不修改 RTL、XDC、PS 软件、host 软件或项目目标。默认 dry-run 不做系统驱动修改；非管理员权限下即使传入 `-Apply` 也会拦截。

新增/更新文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/repair_jtag_drivers_admin.ps1` | `5FEEFA926D992CCEF66D4E92465C5F9C5CBB7850A21C705FB82DDD2FAA630024` | JTAG 驱动修复 dry-run / 管理员执行助手 |
| `tools/run_plan_hardware_resume_safe.ps1` | `9DBE1A4F349B265412D47234E928A22F5BEF7C4D26ABD662723B7F19FC4576CA` | 在 JTAG 失败路径中整合驱动修复 dry-run |

验证 1：默认 dry-run。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\repair_jtag_drivers_admin.ps1
```

结果：

```text
IS_ADMIN=0
XILINX_DRIVER_ROOT_PRESENT=1
XILINX_INSTALL_DRIVERS_CMD_PRESENT=1
XILINX_PCUSB_INSTALL_CMD_PRESENT=1
DIGILENT_INSTALLER_PRESENT=1
DRY_RUN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
TO_APPLY_BOTH=powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\repair_jtag_drivers_admin.ps1 -Apply -InstallPcUsb -InstallDigilent
```

验证 2：非管理员误加 `-Apply`。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\repair_jtag_drivers_admin.ps1 -Apply -InstallPcUsb -InstallDigilent
```

结果：

```text
APPLY=1
INSTALL_PCUSB=1
INSTALL_DIGILENT=1
IS_ADMIN=0
APPLY_BLOCKED_NOT_ADMIN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
```

验证 3：总入口失败路径。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_plan_hardware_resume_safe.ps1 -ComPort COM3 -TriggerModes a_tx_lane0,a_tx_lane1,b_tx_nonzero -StopOnFail
```

结果：

```text
PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_TARGET_COUNT 0
PREFLIGHT_MATCH=VIVADO_MATCH=HW_PREFLIGHT_RESULT FAIL_NO_TARGET
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
USB_DIAG_EXIT=0
DRIVER_REPAIR_DRY_RUN_EXIT=0
DRIVER_REPAIR_DRY_RUN_MATCH=NO_SYSTEM_DRIVER_CHANGE_DONE=1
AUDIT_MATCH=PLAN_AUDIT_SUMMARY pass=7 waiting_hardware=5 missing_or_unknown=0 total=15
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/jtag_driver_repair_20260626_032337.summary.txt` | `FDAD80D9DCAF93B549387C59C86BE22E6F730360B7692DC805B94158E1C5437B` |
| `reports/jtag_driver_repair_20260626_032344.summary.txt` | `5C877C7523A3FBCBE818DD16C2C1ABF39B252E35D14C0B9BB752E3E007117447` |
| `reports/plan_hw_resume_safe_20260626_032426.summary.txt` | `DE643F74CF2E80F68250D115E3AE930EB1BE6B86CE52E9C009E2A456FE0AD81A` |
| `reports/plan_completion_audit_current_20260626.md` | `A3456783ED39D670AF7B27515CAFD20D88F1368AE00F390C28A8A9D135C0DED4` |
| `reports/plan_completion_audit_current_20260626.json` | `2FBC0626E2343A251B3E26C3B04B3F4753CA7C50A363B3E44F1CE2F2BF32FC24` |

判读：

- 当前仍不能宣称 JTAG 已恢复；Vivado 预检仍是 `FAIL_NO_TARGET`。
- 当前可以宣称：驱动修复路径已经固化为可审计脚本，非管理员误操作不会改系统驱动。
- 当前没有烧写 FPGA、没有驱动 TFDU 小板，不触发小板 5 分钟运行限制。

## 31. 2026-06-26 03:30 增加 PS/PC 旁路安全探测入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。新增工具面向 JTAG 暂不可用时的 SD/串口/网络旁路验证，不修改 RTL、XDC、bitstream、PS 软件或 host 协议。

源码依据：

- PS lwIP bridge UART banner：`RF_COMM PS lwIP bridge`
- DHCP/static 后打印：`Board IP: ...`
- TCP 监听提示：`RF TCP bridge listening on port 5001`
- 静态 fallback：`192.168.1.10`
- host smoke：`software/host_client/run_acceptance.ps1 -Mode smoke`，只执行 HELLO/STATUS，不发送 `TX_DATA`

新增/更新文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/probe_ps_uart_boot_safe.ps1` | `E9FD3B76A927E6434B17A12BBB9DB71F38906162E55B7EB66B35ACCB1D27EC42` | 只读 UART 启动探测，捕获 PS bridge / PSPS loopback banner 和 IP |
| `tools/run_ps_pc_board_smoke_safe.ps1` | `C4CD91DD1A3E71AFA8EE686E884DE8F61567DABF044C1A59D128338A89846B94` | 真实板级 TCP smoke 安全包装器；默认需 UART IP，HELLO/STATUS only |
| `tools/audit_plan_completion.py` | `2C592D8E7D485EAF7B3A7907BB80CB49B9B566F91C0D1A6E95D3C693326EAB5A` | 审计中加入 UART probe 和 board smoke wrapper 条目 |

验证 1：UART 只读启动探测。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\probe_ps_uart_boot_safe.ps1 -ComPort COM3 -DurationSeconds 8
```

结果：

```text
COM_PORT_PRESENT=1
UART_OPEN_OK=1
UART_LOG_BYTES=0
MATCH_BRIDGE_BANNER=0
MATCH_PSPS_BANNER=0
MATCH_BOARD_IP=0
MATCH_TCP_LISTEN_5001=0
UART_PROBE_VERDICT=INCONCLUSIVE_NO_UART_TEXT
NO_UART_WRITE_DONE_BY_THIS_SCRIPT=1
NO_FPGA_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
NO_TFDU_DRIVE_DONE_BY_THIS_SCRIPT=1
```

验证 2：board smoke 无 IP 安全退出。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_board_smoke_safe.ps1
```

结果：

```text
LATEST_UART_VERDICT=INCONCLUSIVE_NO_UART_TEXT
SMOKE_BLOCKED_NO_TARGET_HOST=1
NO_TCP_CONNECT_DONE_BY_THIS_SCRIPT=1
NO_TX_DATA_DONE_BY_THIS_SCRIPT=1
NO_FPGA_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
NO_TFDU_DRIVE_DONE_BY_THIS_SCRIPT=1
```

验证 3：静态 fallback dry-run。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_board_smoke_safe.ps1 -UseStaticFallback -DryRun
```

结果：

```text
TARGET_HOST_STATIC_FALLBACK=192.168.1.10
SMOKE_COMMAND=powershell -NoProfile -ExecutionPolicy Bypass -File ...\software\host_client\run_acceptance.ps1 -Mode smoke -TargetHost 192.168.1.10 -Port 5001 -TimeoutSeconds 5
SMOKE_DRY_RUN=1
NO_TCP_CONNECT_DONE_BY_THIS_SCRIPT=1
NO_TX_DATA_DONE_BY_THIS_SCRIPT=1
NO_FPGA_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
NO_TFDU_DRIVE_DONE_BY_THIS_SCRIPT=1
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/ps_uart_boot_probe_20260626_032758.summary.txt` | `6254A379B58A3AB15C792F83202DA5DD7B339CEC16F5359AECC972A8007BA762` |
| `reports/ps_uart_boot_probe_20260626_032758.uart.log` | `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855` |
| `reports/ps_pc_board_smoke_safe_20260626_032930.summary.txt` | `5FD4194044D885C7E03955291458EA3B29C33758D07D97853FBD74667ED50EEC` |
| `reports/ps_pc_board_smoke_safe_20260626_032935.summary.txt` | `B05EB318832F72FE734D0C40F5FFD64EEB5E5B6D05EDFD5A36394EB930B64F81` |
| `reports/plan_completion_audit_current_20260626.md` | `1EB248F94A134CAA3CEE9691F4D18F06F87E890FF471EE174AB3779C0CC7C504` |
| `reports/plan_completion_audit_current_20260626.json` | `D53832A0A52E34381C12730D7CE7CEB7AC01CCEA5B0CE2C7562E76AB30429C19` |

审计更新：

```text
PLAN_AUDIT_SUMMARY pass=9 waiting_hardware=5 missing_or_unknown=0 total=17
P2-UART-TOOL=PASS
P2-SMOKE-TOOL=PASS
P2-BOARD=WAITING_HARDWARE
```

判读：

- 当前可以宣称：无 JTAG 时的 UART/SD 启动旁路探测和真实板级 TCP smoke 安全入口已经准备好。
- 当前不能宣称：开发板已经运行 PS lwIP bridge；最新 UART verdict 是 `INCONCLUSIVE_NO_UART_TEXT`。
- 当前不能宣称：真实 AX7010 PS-to-PC TCP/DHCP smoke 已通过；board smoke 没有发起真实 TCP 连接。

## 32. 2026-06-26 03:33 SD BOOT 镜像一致性审计

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只审计和重建 SD 启动镜像文件，不连接开发板、不烧写 FPGA、不驱动 TFDU 小板，因此不触发 5 分钟运行限制。

新增文件：

| 文件 | SHA256 | 说明 |
|---|---|---|
| `tools/audit_boot_artifacts.py` | `B2B790984D9E9085E912765FD7C05957CA4249B0665403C6736BD6253CE73675` | 解析 BIF，审计 BOOT.BIN、FSBL、bitstream、ELF 哈希、时间戳和 UART marker |

初始审计结果：

```text
BOOT_AUDIT_SUMMARY pass=1 warn=1 fail=0 total=2
BOOT_AUDIT_ITEM package=ps_lwip_bridge status=PASS boot=software\_boot\BOOT.BIN
BOOT_AUDIT_ITEM package=ps_ps_loopback status=WARN_STALE_BOOT_BIN boot=software\_boot_ps_ps_loopback\BOOT.BIN
```

判读：

- `software/_boot/BOOT.BIN` 对应 PS lwIP bridge，组件和 marker 均可追踪，状态 PASS。
- `software/_boot_ps_ps_loopback/BOOT.BIN` 比当前 FSBL/bitstream/ELF 输入旧，不能作为当前 SD 回环镜像使用。

执行 PS-PS loopback BOOT 重建：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\software\ps_ps_loopback\build_boot_image.ps1
```

bootgen 结果：

```text
PSPS_BOOT_IMAGE priority=High affinity=65535
Generating PS-PS loopback BOOT.BIN
[INFO]   : Bootimage generated successfully
PSPS_BOOT_BIN C:\Users\user\Documents\RF_COMM\software\_boot_ps_ps_loopback\BOOT.BIN bytes=2253584 mtime=2026-06-26 03:32:52
```

注意：bootgen 输出了分区地址 overlap warning，但最终明确 `Bootimage generated successfully`。本轮未将 warning 作为阻塞，因为 BOOT 镜像生成成功且后续静态组件审计通过。

重建后审计结果：

```text
BOOT_AUDIT_SUMMARY pass=2 warn=0 fail=0 total=2
BOOT_AUDIT_ITEM package=ps_lwip_bridge status=PASS boot=software\_boot\BOOT.BIN
BOOT_AUDIT_ITEM package=ps_ps_loopback status=PASS boot=software\_boot_ps_ps_loopback\BOOT.BIN
```

当前 BOOT 包哈希：

| 文件 | SHA256 |
|---|---|
| `software/_boot/BOOT.BIN` | `4C753690E35F5D3ED2F611E3D83602BF4A94CE260E34FBF241F24378BBF7C30D` |
| `software/_boot/rf_comm_boot.bif` | `A6E9C4AE4BA31195331A18FD6BB174CAE9E1E9285A5E4F51F8030203C1F51223` |
| `software/_boot_ps_ps_loopback/BOOT.BIN` | `986AA672F467C5461596AD47303C1A2314C48DAAE10A56C3B5DF9FD51946CBA7` |
| `software/_boot_ps_ps_loopback/rf_comm_ps_ps_loopback.bif` | `E84695FA46A3B26925120881D44F92FD119811FCAEDA1D20DE96C351CD5EFA66` |

审计报告：

| 文件 | SHA256 |
|---|---|
| `reports/boot_artifact_audit_current_20260626.md` | `1BC1F3A457EEC67AC1E57B62C0C52CA595026AA38EE9D6C5AAB64872A4B6528A` |
| `reports/boot_artifact_audit_current_20260626.json` | `F4242182FDB823A7E2B05E1ADCDB761868DAAAD6BF9627BEAE4924A52E582ED9` |
| `reports/plan_completion_audit_current_20260626.md` | `6C3304D3D1947AC9F1F835E6FE3FE6292B65C88492961E00A603294551E275D0` |
| `reports/plan_completion_audit_current_20260626.json` | `73B03E5EBE9351F15DB81B4FC6B0E20F779F099346EC3AD864F25D4D5F973808` |

总审计更新：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
BOOT-AUDIT=PASS
```

判读：

- 当前可以宣称：PS lwIP bridge 和 PS-PS loopback 两套 SD BOOT 包均已做组件哈希审计，并且当前输入组件未比 BOOT.BIN 更新。
- 当前不能宣称：SD 启动已经在开发板上验证；最新 UART 旁路仍未看到 PS 输出。

## 33. 2026-06-26 03:35 静态 fallback TCP smoke 尝试与 wrapper 判定修复

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只做 PC 到静态 fallback IP 的 HELLO/STATUS smoke，不发送 `TX_DATA`，不烧写 FPGA，不驱动 TFDU。

前置网络状态：

```text
以太网 IPv4 = 192.168.1.101
目标 fallback IP = 192.168.1.10
```

轻量可达性诊断：

```text
Test-Connection 192.168.1.10 -Count 2 -Quiet => False
Get-NetNeighbor 192.168.1.10 => InterfaceAlias=以太网, LinkLayerAddress=00-00-00-00-00-00, State=Unreachable
```

第一次运行 `tools/run_ps_pc_board_smoke_safe.ps1 -UseStaticFallback -TimeoutSeconds 5` 时发现 wrapper 判定问题：底层 `rf_comm_client.py` 连接超时并在 stderr 输出 `TimeoutError`，但外层 PowerShell 进程退出码被记录为 0，导致 wrapper 误标 `PASS_REAL_BOARD_HELLO_STATUS`。

已修复：

- 只有在 stdout 同时解析到 `ACK` 和 `STATUS_RSP`，且 stderr 不含 `Traceback` / `TimeoutError` / `failed with exit code` 等错误时，才允许标记 `PASS_REAL_BOARD_HELLO_STATUS`。
- 如果 stderr 有失败痕迹，即使外层进程退出码为 0，也强制标记为 `FAIL_OR_NO_BOARD_RESPONSE` 并返回非零。

修复后工具哈希：

| 文件 | SHA256 |
|---|---|
| `tools/run_ps_pc_board_smoke_safe.ps1` | `618B1AA83350D95C446163367EF46427D96BB4A77E10FBA220434CBFFD0E9C23` |

修复后重跑命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_board_smoke_safe.ps1 -UseStaticFallback -TimeoutSeconds 5
```

真实结果：

```text
TARGET_HOST_STATIC_FALLBACK=192.168.1.10
NO_TX_DATA_DONE_BY_THIS_SCRIPT=1
NO_FPGA_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
SMOKE_STDERR=TimeoutError: timed out
SMOKE_STDERR=rf_comm_client.py failed with exit code 1
SMOKE_STDERR_FAILURE_PARSED=1
SMOKE_RESPONSE_OK_PARSED=0
SMOKE_VERDICT=FAIL_OR_NO_BOARD_RESPONSE
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `reports/ps_pc_board_smoke_safe_20260626_033535.summary.txt` | `CCA85400C1B6EFD6E6C01CE08F740E661340297AF97CCCE7579569BD815749CD` |
| `reports/ps_pc_board_smoke_safe_20260626_033535.acceptance.log` | `E94AA4F3CB812D181155ACDE012DA794F1BBF8975D51C3CBD7F69FB85B838EE8` |
| `reports/ps_pc_board_smoke_safe_20260626_033535.acceptance.err.log` | `7E3B3F349B3F3A037E0DA15161D91F697D8BE3FB1E0D01606A2073EA30351CEC` |
| `reports/plan_completion_audit_current_20260626.md` | `81A4AB88BDDBFE77C515D7FFB1A4C17925719B6F354C39B9820A0F3AB77CCE35` |
| `reports/plan_completion_audit_current_20260626.json` | `6E6409166A458A95E93A19D5901752DA1CE4956880406C3AB0941E9CD2BF3705` |

审计状态：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
P2-SMOKE-TOOL=PASS
P2-BOARD=WAITING_HARDWARE
```

判读：

- 当前可以宣称：真实板级 TCP smoke wrapper 的失败判定已修复，不会再把连接超时误标 PASS。
- 当前不能宣称：PS lwIP bridge 已启动或 TCP/DHCP smoke 已通过。
- 当前静态 fallback IP `192.168.1.10` 没有响应，仍需开发板 SD 启动成功或 JTAG 恢复后再继续。

## 34. 2026-06-26 03:39 smoke wrapper 有效退出码补强与重试

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只做 PC 到静态 fallback IP 的 HELLO/STATUS smoke 重试，不发送 `TX_DATA`，不烧写 FPGA，不驱动 TFDU。

补强内容：

- `software/host_client/run_acceptance.ps1` 增加顶层 `trap`，底层 Python 失败时明确退出非零。
- `tools/run_ps_pc_board_smoke_safe.ps1` 保留原始 `SMOKE_EXIT`，并新增 `SMOKE_EFFECTIVE_EXIT`，用于记录 wrapper 根据 stderr 和响应内容修正后的真实判定退出码。

重试命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_board_smoke_safe.ps1 -UseStaticFallback -TimeoutSeconds 1
```

真实结果：

```text
TARGET_HOST_STATIC_FALLBACK=192.168.1.10
NO_TX_DATA_DONE_BY_THIS_SCRIPT=1
NO_FPGA_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
SMOKE_EXIT=0
SMOKE_STDERR=TimeoutError: timed out
SMOKE_STDERR_FAILURE_PARSED=1
SMOKE_RESPONSE_OK_PARSED=0
SMOKE_VERDICT=FAIL_OR_NO_BOARD_RESPONSE
SMOKE_EFFECTIVE_EXIT=1
```

日志哈希：

| 文件 | SHA256 |
|---|---|
| `software/host_client/run_acceptance.ps1` | `C0C9AB13CA9FF9DAED9433ACD735DCADBDAC412F77E36F27C22FBECBD18F0CE3` |
| `tools/run_ps_pc_board_smoke_safe.ps1` | `3B7885B9B1FFF70682098EF054D41FC4402E1621BBB83C479E3B92243CC2AAD7` |
| `reports/ps_pc_board_smoke_safe_20260626_033931.summary.txt` | `9B7554D2CCDFF838AC74497EBAACDB792A6810905AE1EB653851422CCCABF40F` |
| `reports/ps_pc_board_smoke_safe_20260626_033931.acceptance.log` | `7E26F761ABC6D8AF4557524CEAA193F0670127F2F815221342F8CD8C8C9CA023` |
| `reports/ps_pc_board_smoke_safe_20260626_033931.acceptance.err.log` | `FB7E74BD61A61BE0E209D7DADEAE28CB8FB72C3036013099317AECD163079FCB` |
| `reports/plan_completion_audit_current_20260626.md` | `F712BA720306859B7B401B636301D190D7915B45CBEB4BB748C2095ACB5DF030` |
| `reports/plan_completion_audit_current_20260626.json` | `C2ABF8DC1800F0230E5751F6124CC16DF4DFEB4AC5115623589DFC61EBC6609B` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

审计状态：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
P2-SMOKE-TOOL=PASS
P2-BOARD=WAITING_HARDWARE
```

判读：

- 当前可以宣称：真实板级 TCP smoke 的失败判据已经收紧，连接超时会稳定记录为失败。
- 当前不能宣称：PS lwIP bridge 已启动或真实 TCP/DHCP smoke 已通过。
- 当前仍需先恢复 JTAG target，或让开发板从 SD 启动并在 UART 打印 `Board IP` 后再继续真实板级网络验收。

## 35. 2026-06-26 03:42 离线门禁重跑与 plan 条目级审计

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮没有烧写 FPGA，没有运行 Vivado hardware manager，没有发送红外数据，也没有驱动 TFDU。

由于上一轮修改了 `software/host_client/run_acceptance.ps1` 和 `tools/run_ps_pc_board_smoke_safe.ps1` 的失败判据，本轮先重跑 PS/PC 离线门禁，确认 host/offline 路径未回归。

运行命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ps_pc_offline_gates.ps1 -Repeat 64 -PayloadSize 96 -ReconnectCycles 4 -TimeoutSeconds 5
```

结果：

```text
PS_BRIDGE_STATIC_CHECKS_PASS checks=60 dhcp=1 tcp=1 protocol=1 reconnect=1
host_client_unittest Ran 17 tests in 1.203s OK
host_offline_mock_acceptance acceptance PASS
log_acceptance PASS
PS_PC_OFFLINE_GATES_PASS static=1 unittest=1 offline_mock=1
```

同时新增 `tools/audit_plan_items.py`，用于按 `plan.md` 的 P0-1 到 P3、M0 到 M10 逐项审计，避免把 simulation/offline PASS 误记成 hardware PASS。

条目级审计摘要：

```text
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
```

总完成度审计刷新后仍为：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
```

新增/更新证据哈希：

| 文件 | SHA256 |
|---|---|
| `tools/audit_plan_completion.py` | `077366954E77B028FFB49410A76C2F8F7ED30FAD80F9AF652A354AB007CB4C0A` |
| `tools/audit_plan_items.py` | `B5E580B0AE8A2081CF31C66AF05E2FAC4B4F7BB46F0C56429A2C2BE0053B68AC` |
| `reports/ps_pc_offline_gates_20260626_034206.summary.txt` | `DED70DF9F1C3423A276FF97632D32A51D03ABB0069578B2BD54188B420B6968A` |
| `reports/ps_pc_offline_acceptance_20260626_034206/offline_mock_single_lane_20260626_034210.csv` | `40E48C15F1B44B34774258FC8930C01FCCBD3835BB10E4ED35EB239BFC10C00A` |
| `reports/plan_item_audit_current_20260626.md` | `E707B3B4091E73DC53E2951447AD37D0397C4FAD72348C5475269E2B5A68F16B` |
| `reports/plan_item_audit_current_20260626.json` | `31EE6196EF3FBC495E0DE03B72935919E3D6C420A886EBD963D3F02E4692ED79` |
| `reports/plan_completion_audit_current_20260626.md` | `1122C9C2EA0FBA0E501428FBC7C56DD9A995B633E19275B1418B483355C2F9F0` |
| `reports/plan_completion_audit_current_20260626.json` | `BB98656E7D247AF3D755CA8A86196D63B2EA6FAF1529B3A401C0BEF7226E2B85` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

判读：

- 当前可以宣称：host/offline PS-PC 协议工具链在最新 host 脚本改动后仍通过。
- 当前可以宣称：plan 条目级状态已被单独审计，明确区分了 PASS、PARTIAL_SIM_PASS、READY_FOR_HARDWARE、WAITING_HARDWARE 和 NOT_STARTED。
- 当前不能宣称：P0-2/P0-3 的 known-good 硬件回放、P2-2 的真实 PC-to-PC smoke、M6/M8/M9/M10 等硬件/旋转目标已完成。

## 36. 2026-06-26 03:47 JTAG 最新预检与失败路径归档

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只做 JTAG 枚举预检、USB 诊断和驱动修复 dry-run；没有烧写 FPGA，没有进入 matrix，没有驱动 TFDU。

先运行只枚举预检：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check_hw_target.ps1 -ComPort COM3 -JtagFrequencyHz 1000000
```

结果仍为：

```text
SERIAL_PORTS=COM1,COM3
COM_PORT_PRESENT=1
PNP_DEVICE status=OK class=USB name=USB Serial Converter id=USB\VID_0403&PID_6014\210512180081
PNP_DEVICE status=OK class=Ports name=Silicon Labs CP210x USB to UART Bridge (COM3)
VIVADO_PREFLIGHT_EXIT=2
HW_PREFLIGHT_TARGET_COUNT 0
HW_PREFLIGHT_RESULT FAIL_NO_TARGET
```

随后补强 `tools/run_plan_hardware_resume_safe.ps1`：新增 `PREFLIGHT_EFFECTIVE_EXIT`，避免外层 PowerShell 进程码与内部 Vivado preflight 退出码混淆；同时让两份审计脚本引用最新 `plan_hw_resume_safe_*` 归档。

最新集成失败路径归档：

```text
reports/plan_hw_resume_safe_20260626_034713.summary.txt
PREFLIGHT_PROCESS_EXIT=0
VIVADO_PREFLIGHT_EXIT=2
HW_PREFLIGHT_TARGET_COUNT 0
HW_PREFLIGHT_RESULT FAIL_NO_TARGET
PREFLIGHT_EFFECTIVE_EXIT=2
PREFLIGHT_PASS_PARSED=0
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
DRY_RUN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
```

USB/驱动观察：

```text
FTDI6014_PRESENT=1
COM_PORT_PRESENT=1
VID_0403 count=1
VID_10C4 count=1
Digilent count=0
Xilinx count=0
IS_ADMIN=0
```

更新后审计摘要：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
```

新增/更新证据哈希：

| 文件 | SHA256 |
|---|---|
| `tools/run_plan_hardware_resume_safe.ps1` | `64F58D7125EBDBD05B49599B02A922245E7F7A523028F387951D350E63DDCE0F` |
| `tools/audit_plan_completion.py` | `4557CFB19A3DD99ED5C3856EA4A06DF658658B8FE500E081FAB0D30F5B52DF0D` |
| `tools/audit_plan_items.py` | `98C037359546224040CD18E722D1A0C133B9A6128D18BB2BBEF13018B5CF27FF` |
| `reports/hw_target_preflight_20260626_034713.summary.txt` | `E75AE2F5DFB0D830BE81471646FE29D124A58734D24F59E4138F0AB70D2966B5` |
| `reports/plan_hw_resume_safe_20260626_034713.summary.txt` | `65567560D79BB256B00ECDFBB1EA38F6F21E0AE50E903BB7C254D21EAF6F7BC2` |
| `reports/plan_completion_audit_current_20260626.md` | `D2722A12E575252399C09BEC88B662D252F62175B95021455821AE4341F934DD` |
| `reports/plan_item_audit_current_20260626.md` | `86E36B2B990B71FCBD640D221FF50486C392EE9D9D579AB4E7856468E2CB6411` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

判读：

- 当前可以宣称：JTAG 最新预检仍失败在 Vivado target count 为 0，因此所有硬件烧写和 TFDU 运行被安全阻断。
- 当前可以宣称：集成恢复入口会在该失败条件下执行 USB 诊断和驱动 dry-run，并刷新审计，不会误入硬件 matrix。
- 当前不能宣称：P0-2/P0-3 的 known-good 实物回放已经开始或通过。

## 37. 2026-06-26 03:50 JTAG 绑定诊断增强

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮只增强并运行 USB/JTAG 只读诊断，没有安装/重绑驱动，没有烧写 FPGA，没有驱动 TFDU。

修复点：

- `tools/diagnose_jtag_usb.ps1` 现在会完整摘录 FTDI6014 实例的驱动详情，不再受 `pnputil` 中英文输出差异影响。
- 新增 `FTDI6014_BOUND_SERVICE`、`FTDI6014_BOUND_TO_FTDIBUS`、`FTDI6014_DRIVER_PROVIDER_FTDI`、`FTDI6014_DRIVER_ORIGINAL_FTDIBUS`、`FTDI6014_DRIVER_ORIGINAL_WINUSB` 等判据。
- 两份审计报告现在会把最新 `jtag_usb_diag_*.summary.txt` 纳入 JTAG / hardware waiting evidence。

运行命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\diagnose_jtag_usb.ps1 -ComPort COM3 -SkipVivadoSelfCheck
```

关键结果：

```text
COM_PORT_PRESENT=1
FTDI6014_PRESENT=1
FTDI6014_BOUND_SERVICE=FTDIBUS
FTDI6014_BOUND_TO_FTDIBUS=1
FTDI6014_DRIVER_PROVIDER_FTDI=1
FTDI6014_DRIVER_ORIGINAL_FTDIBUS=1
FTDI6014_DRIVER_ORIGINAL_WINUSB=0
PNPUTIL_CONNECTED_MATCH pattern=Digilent count=0
PNPUTIL_CONNECTED_MATCH pattern=Xilinx count=0
NO_PROGRAMMING_DONE_BY_THIS_SCRIPT=1
```

审计摘要保持：

```text
PLAN_AUDIT_SUMMARY pass=10 waiting_hardware=5 missing_or_unknown=0 total=18
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
```

新增/更新证据哈希：

| 文件 | SHA256 |
|---|---|
| `tools/diagnose_jtag_usb.ps1` | `4C0C99F9B797CD03C877C7289A8E1984D9F5A15D1521CB6BB99FCFE2CA2742FC` |
| `tools/audit_plan_completion.py` | `0D7B44DE611BD782328FF95484D0C51EBDE05B4927344DA75B7E085B411DA619` |
| `tools/audit_plan_items.py` | `C4DCFF318EAFBA08F871164AF24EB8621EAABEF336C458966534AEF8DFB3A928` |
| `reports/jtag_usb_diag_20260626_035039.summary.txt` | `D07FE1BF67BCE34C82CC579B8FF79EF447E22E8BEBF95CDB61152E0E06F90FFF` |
| `reports/jtag_usb_diag_20260626_035039.ftdi6014_USB_VID_0403_PID_6014_210512180081.drivers.log` | `C5F0ED0CB28DAAE0056573610B820993A32E2FAACD0DC327BC460338E7DDDF37` |
| `reports/plan_completion_audit_current_20260626.md` | `15377508D2016E0AFBD261BF38665D1AAE6D9FC5ABE9B11E78E7F3282B9BC991` |
| `reports/plan_item_audit_current_20260626.md` | `49545A3EA0E85E4DB60106BD6F1607584C22FF1D7302A3119D8E8896C31613EE` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

判读：

- 当前可以宣称：Windows 看到的 FTDI6014 设备绑定为 FTDI `FTDIBUS` / `ftdibus.inf`，而不是当前摘要中可见的 Xilinx/Digilent JTAG 设备。
- 当前仍不能宣称：Vivado 能枚举到 Zynq target。
- 下一步若要恢复硬件测试，应检查实际插入的是不是板卡 JTAG 口、板卡 JTAG/BOOT 模式、供电状态，以及是否需要管理员 PowerShell 运行 Xilinx/Digilent cable driver 修复。

## 38. 2026-06-26 03:53 JTAG 驱动恢复后自动接续验证入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮新增并验证一个 JTAG 驱动恢复包装脚本；验证只跑 dry-run 和非管理员阻断路径，没有改系统驱动，没有烧写 FPGA，没有驱动 TFDU。

新增工具：

```text
tools/run_jtag_driver_recovery_then_resume.ps1
```

用途：

- 默认 dry-run：只打印将执行的驱动修复和安全恢复命令。
- `-Apply` 且管理员运行：先调用 `tools/repair_jtag_drivers_admin.ps1` 安装/重扫 cable driver，再自动调用 `tools/run_plan_hardware_resume_safe.ps1`。
- 如果 JTAG 仍不可见，`run_plan_hardware_resume_safe.ps1` 会停在 preflight，不会烧写。
- 如果 JTAG 恢复可见，才会进入后续 2-lane matrix；单次硬件 runner 的 `finally` 会烧入 shutdown bitstream。

dry-run 自检结果：

```text
reports/jtag_recovery_then_resume_20260626_035316.summary.txt
APPLY=0
DRY_RUN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
NO_RESUME_RUN_DONE=1
TO_RUN_ELEVATED=powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_jtag_driver_recovery_then_resume.ps1 -Apply -InstallPcUsb -InstallDigilent -StopOnFail
TO_LAUNCH_ELEVATED=powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_jtag_driver_recovery_then_resume.ps1 -LaunchElevated -InstallPcUsb -InstallDigilent
```

非管理员 `-Apply` 防误操作验证：

```text
reports/jtag_recovery_then_resume_20260626_035326.summary.txt
APPLY=1
IS_ADMIN=0
APPLY_BLOCKED_NOT_ADMIN=1
NO_SYSTEM_DRIVER_CHANGE_DONE=1
NO_RESUME_RUN_DONE=1
```

新增证据哈希：

| 文件 | SHA256 |
|---|---|
| `tools/run_jtag_driver_recovery_then_resume.ps1` | `6641B0C631D059AEB5C5716ED3FB86FAC04D5C0826DC0448AA41A518D0DE188A` |
| `reports/jtag_recovery_then_resume_20260626_035316.summary.txt` | `2A69A5B26E9BA8EB6FD6DED4146AF5CE9ED3EF9E8117B4EBEF72EE3F283D407B` |
| `reports/jtag_recovery_then_resume_20260626_035326.summary.txt` | `1CD8DC3E4D18F18C3A4FA49B0FBDF26239ADC1443331E610FD7CBFE09E71B1BF` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

判读：

- 当前可以宣称：JTAG 驱动恢复和硬件验证已经有单入口串联脚本，默认安全，且非管理员误执行 `-Apply` 会被阻断。
- 当前不能宣称：驱动已实际重装、JTAG 已恢复或硬件回放已执行。

## 39. 2026-06-26 03:57 P0 known-good 回放安全入口

本轮继续遵守硬约束，未修改 `项目约束(目标）.txt`。本轮新增 P0 known-good 回放包装脚本，并加固 lane0 单次 runner 的 JTAG preflight 判据。验证过程停在 JTAG preflight，未烧写 FPGA，未驱动 TFDU。

安全加固：

- `tools/run_lane0_hw_once_safe.ps1` 新增 JTAG preflight，且解析 `HW_PREFLIGHT_RESULT PASS` + `HW_PREFLIGHT_ZYNQ`，不再只依赖外层 PowerShell 进程码。
- 新增 `tools/run_p0_known_good_replay_safe.ps1`，用于串行执行 P0-2 lane0 known-good 回放和 P0-3 2-lane known-good 回放。
- P0 wrapper 当前 recipe：
  - lane0：`IR_LANE_COUNT=1,CNT_CHIP_MAX=7,lane_mask=0x1,session=0x2201,payload_bytes=247`
  - 2-lane：`IR_LANE_COUNT=2,B_SESSION_ID=0x2203,lane_mask=0x3,trigger=b_tx_nonzero`

dry-run 自检：

```text
reports/p0_known_good_replay_safe_20260626_035632.summary.txt
DRY_RUN=1
DRY_RUN_NO_PREFLIGHT_DONE=1
DRY_RUN_NO_HARDWARE_DONE=1
```

lane0 runner 单独阻断验证：

```text
reports/lane0_hw_loopback_safe_20260626_035717.summary.txt
PREFLIGHT_EFFECTIVE_EXIT=2
PREFLIGHT_PASS_PARSED=0
PREFLIGHT_BLOCKED_NO_PROGRAMMING=1
```

P0 wrapper 阻断验证：

```text
reports/p0_known_good_replay_safe_20260626_035743.summary.txt
PREFLIGHT_EFFECTIVE_EXIT=2
PREFLIGHT_PASS_PARSED=0
P0_REPLAY_BLOCKED_NO_PROGRAMMING=1
```

审计状态：

```text
PLAN_ITEM_AUDIT_SUMMARY not_started=4 partial_sim_pass=2 pass=5 ready_for_hardware=2 waiting_hardware=11 total=24
```

新增/更新证据哈希：

| 文件 | SHA256 |
|---|---|
| `tools/run_lane0_hw_once_safe.ps1` | `F14A0AF00B1C9EB98F6009C0759C7B81ABFF8B4E9EE3D7FC23D5AB6BB93F2A7B` |
| `tools/run_p0_known_good_replay_safe.ps1` | `41751DEB604F7199296ADF056CDA272D4721B20A17C11230D83ED50EBF56BD17` |
| `tools/audit_plan_items.py` | `1F71BA7DA71F25C744C24DBBE95E202166F9366FA67FC56328511ED7EE015974` |
| `reports/p0_known_good_replay_safe_20260626_035632.summary.txt` | `7685B048708D5A6B315AAB817D6360D78AC325A15B60E182502A3FD039C2361B` |
| `reports/p0_known_good_replay_safe_20260626_035743.summary.txt` | `A1B9B02CC974CD7C02FDBAE11FC89A109C3A48B7FF3B3E0B1973A13D8DA31B0E` |
| `reports/lane0_hw_loopback_safe_20260626_035717.summary.txt` | `3D0A43F187CB7EBE77A7B87231F091DA38B6F3463BC8B6F77C9E0E125A43A922` |
| `reports/plan_item_audit_current_20260626.md` | `B501CDC0970E3AC83B9139F5224512C463AD9CDE97B59C76C03ED8B40A4BE5D1` |
| `reports/plan_completion_audit_current_20260626.md` | `271C3E9E42089E8FC97325C6BAE9060F05D4F743954114F6E29895BC49F61FDD` |
| `项目约束(目标）.txt` | `CFF1A17EE77BBAF90080CF4F97E5920E961AEFAE3B6752F080E35FCF4D4B1F11` |

判读：

- 当前可以宣称：P0-2/P0-3 回放已经有安全执行入口，并且当前 JTAG 不可见时会阻断在烧写前。
- 当前不能宣称：P0-2/P0-3 回放已经在硬件上执行或通过。
